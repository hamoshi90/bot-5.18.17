# 🚀 دليل النشر والتشغيل

## 1) التشغيل المباشر (VPS)

```bash
# المكتبات
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# الإعدادات
cp .env.example .env
nano .env          # التوكن + معرف الأدمن + المفاتيح

# تجربة
python bot.py
```

### تشغيل دائم عبر systemd
```bash
sudo cp bot.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now bot
sudo journalctl -u bot -f      # متابعة اللوج
```

## 2) تشغيل Docker [NEW 17]

```bash
cp .env.example .env && nano .env
docker compose up -d --build     # يشغّل البوت + الـ API معاً
```
- البيانات تُحفظ في volume باسم `bot-data`
- البوت يكتب على `/data/bot.db` داخل الحاوية

## 3) Web API و Mini App [NEW 14/15]

```bash
# ضع API_KEY في .env أولاً
uvicorn api:app --host 0.0.0.0 --port 8000
```

| نقطة النهاية | الوصف | حماية |
|---|---|---|
| `GET /api/health` | فحص الحالة | عامة |
| `GET /api/user/{id}` | بيانات مستخدم | X-API-Key |
| `GET /api/user/{id}/transactions` | عملياته | X-API-Key |
| `POST /api/balance/adjust?telegram_id=&amount=` | تعديل رصيد | X-API-Key |
| `GET /miniapp/{id}?t=` | صفحة Mini App | توقيع |

**ربط Mini App بتلجرام:** BotFather →Bot Settings → Menu Button → الرابط:
`https://YOUR-DOMAIN/miniapp/{telegram_id}?t={التوقيع}`
(للإنتاج: فعّل HTTPS عبر nginx + تحقق كامل من initData).

## 4) PostgreSQL [NEW 18]

عند نمو المشروع (آلاف العمليات المتزامنة):

```bash
pip install "psycopg[binary]"
export POSTGRES_DSN="postgresql://user:pass@host:5432/dbname"
python migrate_to_postgres.py
```

السكربت يرحّل كل الجداول + يضبط العدادات تلقائياً.

لتشغيل البوت نفسه على PG، عدّل `get_db()` في `bot.py`
لتستخدم `asyncpg`Pool، وغيّر العلامات `?` إلى `$1, $2...`
(تغيير موضعي واحد + نفس بقية الكود).

## 5) ملفات مهمة

| الملف | الوظيفة |
|---|---|
| `secret.key` | مفتاح تشفير كلمات المرور — **احتفظ به ولا تفقده!** |
| `bot.db.backup` | آخر نسخة احتياطية (تُرسل للأدمن يومياً أيضاً) |
| `bot_original.py` | الكود الأصلي قبل الفحص والإصلاح |

## 6) المهام الدورية التلقائية

كل 10 دقائق يشغّل البوت:
- 🗄 نسخة احتياطية (مرة يومياً → خاص الأدمن)
- 📅 الملخص اليومي (مرة يومياً → خاص الأدمن)
- ⌛ إنهاء الطلبات المعلقة الأقدم من 24 ساعة

## 7) بوابة الدفع والويب هوك [NEW 35/36]

### شحن تلقائي (Webhook)
```bash
# في .env
PAYMENT_WEBHOOK_SECRET=<مفتاح قوي>
```
| البند | القيمة |
|---|---|
| نقطة الاستقبال | `POST /api/webhooks/payment` |
| التوقيع | ترويسة `X-Signature` = HMAC-SHA256 (hex) لجسم الطلب الخام |
| الحمولة | `{"event_id","telegram_id","amount","event":"payment.succeeded"}` |
| التكرار | نفس `event_id` → `200 {"status":"duplicate"}` بدون احتساب |
| التعارض | نفس id بمحتوى مختلف → `409` |
| أحداث أخرى | (`payment.refunded`...) → `200 ignored` |

### أوامر الدفع (Payouts)
- اعتماد أي سحب **ينشئ أمر دفع تلقائياً داخل نفس معاملة الخصم**
- الإدارة تعالجها من: 💳 الشحن والسحب → 🚀 المدفوعات
  - ✅ مدفوعة = تم التحويل فعلياً
  - ❌ فشلت = **إرجاع تلقائي للرصيد** (مرة واحدة فقط)
- API للمواقع: `GET/POST /api/payouts` + `mark_paid` / `mark_failed`
  - يدعم ترويسة `Idempotency-Key` لمنع إنشاء أمرين لنفس الطلب

## 8) لوحة تحكم الأدمن المطوّرة [NEW 37..46]

### الميزات الجديدة
| # | الميزة | الوصول |
|---|---|---|
| 37 | بحث مستخدم (ID أو حساب الموقع) + كارت بإجراءات سريعة + قائمة بترقيم صفحات | صلاحية users |
| 38 | طابور موحد (طلبات/مدفوعات/تذاكر) + تذاكر دعم مع رد وإغلاق + مراسلة مستخدم | الموظفون |
| 39 | الصلاحيات بلوحة أزرار (تبديل/حفظ/إلغاء) — اختيار 🌐 الكل يفرغ البقية والعكس | الأدمن فقط |
| 40 | إيقاف مشرف مؤقت ⏸ (يُجمّد صلاحياته فوراً حتى التشغيل) | الأدمن فقط |
| 41 | سقوف سحب يومية: لكل مستخدم + إجمالي (0 = معطّل) — تُفحص عند إدخال مبلغ السحب | ⚙️ الحدود |
| 42 | KPI حي: مستخدمون/نشاط اليوم/شحن/سحب/معلقات/تذاكر + زر 🔄 تحديث | الموظفون |
| 43 | تصدير ZIP شامل: users + transactions + finance_requests + admin_audit (CSV بترميز Excel) + نسخة قاعدة البيانات | الأدمن فقط |
| 44 | قائمتان فرعيتان: ⚙️ إعدادات البوت + 🧰 أدوات الإدارة | الموظفون |
| 45 | صيانة مجدولة `HH:MM-HH:MM` بتوقيت UTC (تدبر منتصف الليل؛ `0` للتعطيل) — البوت يعتبر نفسه متوقفاً خلالها | الأدمن فقط |
| 46 | تنبيهات ذكية (كل 10 دقائق): أمر دفع معلق +12س، سحب ضخم ≥ عتبة التأكيد، تراكم >20 طلباً — كل تنبيه مرة واحدة | → خاص الأدمن |

### زر الدعم للمستخدمين
- زر «📞 الدعم» في القائمة الرئيسية: رسالة المستخدم تصل الأدمن كتذكرة بأزرار ✉️ رد / ✖ إغلاق
- رد الإدارة يصل المستخدم بلغته، والتذكرة تُعلَّم answered (لا يمكن إغلاقها مرتين)

### ملاحظات تشغيلية
- جدول جديد: `support_tickets` (+ فهرس) وعمود `supervisors.suspended` — يُنشآن تلقائياً عند الإقلاع
- إذا فعّلت سقفاً يومياً فتأكد من مطابقة `daily_withdraw_total` لرصيد بوابة الدفع الفعلي لديك
- سقوف اليوم تُحسب بتوقيت UTC وتشمل السحوبات **المعتمدة + المعلقة** معاً

## 9) حزمة المظهر والأدمن والمستخدم [UI/ADM/USR]

### المظهر [UI]
| # | الميزة | التفاصيل |
|---|---|---|
| UI-1 | شريط حالة حي | أعلى لوحة الإدارة/الإعدادات/الأدوات/الطابور: 🟢/🔴 + معلقات + تذاكر |
| UI-2 | كلمة سر خلف Spoiler | زر 👁 إظهار + حذف تلقائي بعد 30 ثانية + إخفاء فوري |
| UI-3 | روابط القائمة | ⚙️ الإعدادات → 🔗 روابط القائمة: `قناة \| موقع \| دعم` (0 للمسح) |
| UI-4 | شاشة صيانة أنيقة | أثناء النافذة: «نطوّر الخدمة + نعود HH:MM UTC» (مرة/60ث لكل مستخدم) |
| UI-5 | ترقيم موحد | «صفحة x/y» + زر رقم الصفحة في: المستخدمون/التذاكر/السجل/الأكواد |

### الأدمن [ADM]
| # | الميزة | التفاصيل |
|---|---|---|
| ADM-1 | دفعة أكواد | 🎁 الهدايا → دفعة أكواد: `عدد مبلغ` (حتى 50) + عرض «🚫 ملغى» |
| ADM-2 | موافقة مزدوجة | ⚙️ → 👥 موافقة مزدوجة: سحب ≥ العتبة من مشرف يبقى «awaiting_admin» حتى اعتمادك (يرد عليك برسالة بأزرار) — 0 للتعطيل |
| ADM-3 | إيقاف الخدمات | ⚙️ → ⏸️ إيقاف الخدمات: إيداعات/سحوبات منفرداً + سبب يظهر للمستخدم |
| ADM-4 | ZIP يومي | ⚙️ → 📦 تصدير ZIP يومي: يصلك بعد 09:00 UTC مرة كل يوم |
| ADM-6 | نسخة الآن | 🧰 → 🗄 نسخة احتياطية الآن + فحص سلامة (عدّادات وحجم وآخر نسخة) |

### المستخدم [USR]
| # | الميزة | التفاصيل |
|---|---|---|
| USR-1 | تحويل رصيد P2P | الخدمات → 💸 تحويل: ID → مبلغ → تأكيد؛ سقف يومي «transfer_daily_cap» من ⚖️ الحدود (0=معطل) |
| USR-2 | كشف PDF | 📜 السجل → 🧾 كشف PDF: آخر 30 عملية + الرصيد |
| USR-3 | تذاكري | زر في القائمة: آخر 10 تذاكر بحالتها (مفتوحة/مُجيبة/مغلقة) |
| USR-4 | إهداء رصيد | 🎁 → 🎟 اصنع كود لصديق: خصم فوري + استرداد تلقائي بعد 7 أيام إن لم يُستخدم |

### أعمدة/جداول جديدة
- عمود `gift_codes.created_by` (يُضاف تلقائياً عند الإقلاع، وموجود في migrate_to_postgres.py)
- حالة طلب جديدة: `awaiting_admin` (الموافقة المزدوجة)
- مفاتيح إعدادات جديدة: `link_channel/link_site/link_support`, `deposits_paused/pause_reason_dep`, `withdraws_paused/pause_reason_wd`, `dual_approve_threshold`, `export_zip_daily`, `transfer_daily_cap`

## 10) الجولة الثالثة [USR2 / ADM2]

### المستخدم [USR2]
| # | الميزة | التفاصيل |
|---|---|---|
| USR2-1 | 📋 طلباتي المالية | من السجل: كل الطلبات بحالتها (معلق/مقبول/مرفوض/منتهي/بانتظار الإدارة) بترقيم صفحات |
| USR2-2 | 🤝 إحالاتي + مشاركة | من 📊 إحصائياتك: من انضم عبرك + أرباحك + زر مشاركة الرابط |
| USR2-3 | 💰 حملة مكافأة الإيداع | ⚙️ → 🎁 مكافأة الإيداع: `نسبة حد` (مثال `10 500`) — شارة العرض تظهر في شاشة الشحن، `0` لإنهائها |
| USR2-4 | 🎟 كود إعفاء عمولة | 🎁 → 🎟 كود إعفاء: `CODE عدد` — المستخدم يفعّله من شاشة الشحن وشحنته القادمة بلا عمولة (استهلاك ذرّي داخل معاملة الاعتماد) |
| USR2-5 | ⚡ سحب سريع | زر «آخر مبلغ» في شاشة السحب (من آخر طلب سحب فعلي) |
| USR2-6 | 🔍 فلتر السجل | الكل / شحن / سحب / تحويلات / هدايا |
| USR2-7 | ⭐ تقييم الدعم | بعد رد الإدارة تصل أزرار ⭐1-5 — المتوسط في KPI |

### الأدمن [ADM2]
| # | الميزة | التفاصيل |
|---|---|---|
| ADM2-1 | 🎯 بث مستهدف | بعد اختيار الرسالة: الكل / نشطوا اليوم / رصيد ≥ حد / عربي / English |
| ADM2-2 | 🗒 ملاحظات على مستخدم | من الكارت: ملاحظات سرية تظهر لكل الموظفين (جدول user_notes) |
| ADM2-3 | 💼 حصة مالية للمشرف | 🛡 → 💼 حصة: `tid amount` — ما تجاوز حصته اليومية يرفع تلقائياً لموافقتك (0 = غير محدودة) |
| ADM2-4 | 📊 تقرير P&L شهري | أول تشغيل بعد 09:00 كل شهر: إيداعات/سحوبات/عمولة محصلة/مدفوعات/استردادات/هدايا + مقارنة بالشهر السابق |
| ADM2-5 | 🧾 سجل الويب هوك | 🧰 → آخر 20 حدثاً بحالتها (✅/♻️/⚠️) لتشخيص «شحنت وما وصل» |
| ADM2-6 | 🤖 حماية تلقائية | ⏸️ إيقاف الخدمات → تفعيلها: إيقاف السحوبات تلقائياً عند تراكم >20 طلباً أو فشل 3 مدفوعات متتالية + تنبيه |
| ADM2-7 | 🏋️ أداء المشرفين | 🛡 → أداء المشرفين: اعتمادات/رفض/مُحالة للأدمن + متوسط زمن المعالجة (7 أيام) |

### بنية جديدة
- جداول: `user_notes`, `promo_codes` | أعمدة: `finance_requests.promo_code`, `supervisors.daily_quota`, `support_tickets.rating`
- مفاتيح إعدادات: `bonus_percent/bonus_max`, `last_pl_month`, `auto_protect`
- كلها تُنشأ تلقائياً عند الإقلاع وموجودة في migrate_to_postgres.py

## 11) الجولة الرابعة [ADM3]

### غرفة المراقبة والأمان [ADM3]
| # | الميزة | التفاصيل |
|---|---|---|
| ADM3-1 | 🔐 رمز تأكيد (PIN) | ⚙️ → 🔐 رمز التأكيد: 4-8 أرقام (0 = إزالة). بعد تفعيله تُحمى العمليات الحساسة: اعتماد/رفض سحب، تعديل رصيد، ZIP، تأكيد البث. الموظف يفتح القفل بإدخال الرمز مرة واحدة لمدة 15 دقيقة (زر «🔓 فتح القفل» يظهر عند الحاجة). معرفة الرمز = التصريح؛ الإعداد/الإزالة للأدمن فقط. التخزين SHA-256 |
| ADM3-2 | 🖥 غرفة المراقبة | ⚙️ → 🖥 غرفة المراقبة: chat_id (0 = تعطيل) مع اختبار وصول فوري. تُنسخ إليها آلياً: كل الإشعارات المالية، التذاكر الجديدة والردود، تنبيهات الحماية والصيانة، التقارير اليومية/الشهرية/الأسبوعية. تُتجاهل تلقائياً إذا تساوت مع chat الأدمن أو كانت غير رقمية |
| ADM3-4 | 🕐 آخر نشاط المشرفين | 🛡 قائمة المشرفين تعرض «🕐 آخر نشاط» لكل مشرف (من آخر طلب مالي عالجه) |
| ADM3-8 | 🙈 إخفاء الأرصدة عن المشرفين | ⚙️ → 🙈 إخفاء الأرصدة: في كارت المستخدم وقائمة المستخدمين يرى المشرف «•••» بدل الرصيد، والأدمن يرى دائماً |
| ADM3-11 | 🧹 تنظيف البيانات القديمة | 🧰 → 🧹 تنظيف: عدد الأيام (10-3650) → معاينة أعداد الصفوف القابلة للحذف (أحداث الويب هوك / سجل التدقيق / التذاكر المغلقة والمُجابة) → تأكيد. التذاكر المفتوحة تبقى دائماً |

### الأدوات المالية والبث [ADM3]
| # | الميزة | التفاصيل |
|---|---|---|
| ADM3-3 | 📡 رسالة اختبار | 🧰 → 📡 اختبار: رسالة تشخيصية فورية للأدمن (خاص + غرفة المراقبة) مع زر نتيجة |
| ADM3-5 | 💸 ميزانية السحوبات اليومية | ⚙️ → 💸 الميزانية: أي اعتماد سحب يجعل إجمالي مدفوعات اليوم (معلق+قيد+مدفوع) يتجاوزها يُرفض برسالة «يتجاوز ميزانية اليوم» ويُعاد الزر للمعالجة. تُرفض حتى من الأدمن. `0` = تعطيل |
| ADM3-6 | ♻️ تراجع عن تعديل رصيد | كارت المستخدم → ♻️ تراجع: آخر 5 تعديلات يدوية مع زر تراجع لكل واحد. التراجع = قيد معاكس على الرصيد + صف في السجل الشخصي للمستخدم «تراجع عن #id» + علامة `undone` (لا يُقبل تكرار). الجدول `balance_adjustments` |
| ADM3-7 | 🧮 محاكي الشحن | 🧰 → 🧮 المحاكي: `مبلغ نسبة_عمولة نسبة_مكافأة` → يعرض الصافي النهائي للمستخدم (المبلغ − العمولة + المكافأة حتى السقف) |
| ADM3-9 | 💬 ردود جاهزة للتذاكر | 🛡 → 💬 القوالب: إضافة/حذف قوالب (عنوان ≤40 حرفاً). في شاشة التذكرة زر «💬 قوالب الرد» ثم «استخدام» يرسل الرد فوراً ويغلق التذكرة كـ answered + تدقيق. الجدول `canned_replies` |
| ADM3-10 | 🗓 بث مجدول | شاشة «جاهز للبث» → 🗓 جدولة: `HH:MM` بتوقيت UTC → يُرسل تلقائياً مرة واحدة عند حلول الوقت لنفس الشريحة مع تقرير نتائج. الإلغاء قبل التنفيذ من شاشة البث كالمعتاد |
| ADM3-12 | 📊 تقرير أسبوعي تلقائي | أول تشغيل بعد 09:00 UTC يوم الاثنين: شحن/سحب الأسبوع + مستخدمون جدد + تذاكر + متوسط الرضا، للأدمن وغرفة المراقبة |

### بنية جديدة
- جدول: `balance_adjustments` (id, admin_id, target_tid, amount, note, undone, created_at) | `canned_replies` (id, title, body, created_at)
- مفاتيح إعدادات: `admin_pin_hash`, `pin_unlocked`, `mirror_chat_id`, `staff_hide_balance`, `payout_budget_daily`, `scheduled_broadcast`, `last_sched_broadcast`, `last_weekly_report`
- كلها تُنشأ تلقائياً عند الإقلاع وموجودة في migrate_to_postgres.py

## 12) الجولة الخامسة [ADM4 / USR4]

### لوحة الأدمن [ADM4]
| # | الميزة | التفاصيل |
|---|---|---|
| ADM4-1 | 🏷 وسوم المستخدمين | من الكارت → 🏷 الوسوم: حتى 3 وسوم (≤20 حرفاً) لكل مستخدم. الفلترة من 👥 → 🏷 فلترة بالوسم (حتى 20 نتيجة). الشارة تظهر في الكارت تلقائياً |
| ADM4-2 | 🏆 مسابقة الإحالات | 🧰 → 🏆: مدة (1-90 يوم) + جوائز `100,50,25` (حتى 5 مراكز). شاشة صدارة حية ضمن الفترة. عند الانتهاء (تلقائياً بالمهام أو «⏹ إنهاء ومنح») يُرسل لكل فائز كود هدية بقيمة مركزه + ملخص لك وغرفة المراقبة |
| ADM4-3 | ⚡ اعتماد تلقائي | ⚙️ → ⚡: قائمة ثقة ( حتى أي عدد) + سقف مبلغ + تفعيل. سحب عضو القائمة تحت السقف يُعتمد ويُدفع تلقائياً مع إبلاغه — حارس الميزانية ADM3-5 يبقى ساري الأثر (العترض يبقى pending للفريق) |
| ADM4-4 | 📣 لوحة الإعلانات | 🧰 → 📢: نشر إعلان (نص ≤800 + مدة 1-365 يوم) وحذف. يظهر للمستخدمين بزر «📢 الإعلانات» بالقائمة الرئيسية مع 🆕 لغير المقروء وتتبع قراءة لكل مستخدم، وانتهاء تلقائي |
| ADM4-5 | 🚨 كشف الشواذ | ⚙️ → 🚨: ثلاث عتبات `حد_الإيداع عدد_سحب/ساعة حد_إيداع_الجديد` (صفر = تعطيل). تنبيه فوري (أدمن + مرآة) عند: إيداع مفرد ضخم، بلوغ عدد طلبات سحب/ساعة (تنبيه واحد)، إيداع فوري ≤10 دقائق بعد التسجيل. يرصد شحن الويب هوك والاعتماد اليدوي معاً |
| ADM4-6 | 🧾 تقرير محاسبي PDF | 🧰 → 🧾: تقرير الشهر الحالي (الإيداعات/السحوبات المعتمدة/المدفوعات/العملاء الجدد + أفضل 10 ممولين) PDF للأدمن وغرفة المراقبة |

### المستخدمين [USR4]
| # | الميزة | التفاصيل |
|---|---|---|
| USR4-1 | 💎 مستويات الولاء | ⚙️ → 💎: `عتبة_الفضي عتبة_الذهبي خصم_الفضي% خصم_الذهبي%`. الترقية حسب إجمالي الإيداعات؛ الخصم يُطبق على عمولة الشحن تلقائياً. الشارة + «باقي X للمستوى التالي» في البروفايل وفي حاسبة الشحن |
| USR4-2 | 📒 دفتر وجهات السحب | شاشة السحب: حفظ حتى 3 وجهات (اسم + رقم) بعرض مقنّع «•••XXXX»، اختيار بألمسة يُرفق وجهتها بتأكيد الطلب وإشعار الفريق المالي، والفريق يراها كاملة من الكارت (📒 وجهاته) |
| USR4-3 | 🔥 الحضور اليومي | ⚙️ → 🔥: `تشغيل(0/1) المبلغ_الأساسي سقف_السلسلة`. زر «🔥 حضور اليوم» بالبروفايل: اليوم n = الأساسي × n حتى السقف، مرة كل 24 ساعة (حجز ذري)، الغيوم تكسر السلسلة، وتُسجل كعملية bonus |
| USR4-4 | 📈 منحنى رصيدي | من السجل → 📈: PNG لرصيد آخر 30 يوماً معاد بناؤه من سجل العمليات (يدعم كل الأنواع بما فيها التعديلات اليدوية بالإشارة) |
| USR4-5 | 🧮 حاسبة الشحن | شاشة الخدمات → 🧮: يرسل المبلغ فيرى العمولة (بخصم ولائه) والصافي ومكافأة الحملة (بسقفها) والإجمالي — بنفس معادلة الاعتماد الحقيقية — وزر «💰 شحن هذا المبلغ» |
| USR4-6 | 📦 بياناتي | من السجل → 📦: CSV (UTF-8-BOM) بكل عملياته + ملخص (الرصيد، إجمالي الشحن/السحب، مستوى الولاء). مرة كل 24 ساعة لكل مستخدم |

### بنية جديدة
- جداول: `user_tags`, `announcements`, `announcement_reads`, `checkins`, `auto_wd_whitelist`, `payout_accounts`, `my_exports`
- مفاتيح إعدادات: `loyal_s2/s3`, `loyal_d2/d3`, `checkin_enabled/base/cap`, `contest_start/end/prizes/granted`, `auto_wd_enabled/cap`, `anom_dep_max`, `anom_wd_count`, `anom_new_dep`
- مهام دورية أضيفت: `contest_if_due()` (منح الجوائز عند انتهاء المدة)
- كلها تُنشأ تلقائياً عند الإقلاع وموجودة في migrate_to_postgres.py

## 13) التحديث الآمن — deploy.sh

سكربت نشر مستقل (لا يُعدّل كود البوت، أداة تشغيل خارجية):

```bash
bash deploy.sh bot_project_v5.zip              # من حزمة
bash deploy.sh bot.py                          # أو ملف مفرد
bash deploy.sh pkg.zip --docker               # لأنظمة Docker
bash deploy.sh pkg.zip --unit mybot --pip     # وحدة systemd باسم آخر + تثبيت المتطلبات
```

ما يفعله بالترتيب:
1. فك الحزمة وفحص سلامتها + **فحص صيغة Python لكل ملف قبل لمس أي شيء** (فشل = توقف فوري والبوت يعمل كما هو)
2. نسخة احتياطية مطوّرة الزمن في `backups/pre_deploy_<TIME>/` تشمل `bot.db` و`secret.key` و`.env` وكل الملفات المستبدلة
3. إيقاف الخدمة → نسخ الملفات الجديدة → (اختياري `--pip`) → تشغيل
4. فحص صحة دوري: أي سقوط = **رجوع تلقائي (rollback)** للنسخة السابقة وإعادة التشغيل مع تقرير

محميات ثابتة: `.env` و `bot.db` و `secret.key` لا تُنسخ من الحزمة ولا تُمسّ أبداً حتى لو وُجدت داخلها. سجل كل نشر في الطرفية، وعودة يدوية: `cp -a backups/pre_deploy_XXX/bot.py . && systemctl restart bot`.

## 14) التشغيل على ويندوز — start_bot.bat

ضع `start_bot.bat` بجانب `bot.py` (هكذا هو داخل الحزمة) وانقر عليه مرتين:

- **أول تشغيل**: ينشئ بيئة افتراضية `.venv` + يثبت المكتبات + ينسخ `.env` + يولّد `API_KEY` و`PAYMENT_WEBHOOK_SECRET` قوية آلياً (عبر `_env_setup.py`) + يفتح المفكرة مرة واحدة لسطرين فقط: `TELEGRAM_BOT_TOKEN` و`ADMIN_USER_ID` — احفظ وأغلق فيكمل التشغيل.
- **كل تشغيل لاحق**: يعمل مباشرة، ولو انهار البوت يعيد تشغيله تلقائياً بعد 5 ثوانٍ (بعد 5 انهيارات متتالية يتوقف ويعرض الأخطاء بدل حلقة جنونية).
- **الإيقاف**: أغلق النافذة (أو Ctrl+C ثم Y).
- متطلب وحيد: Python 3.11+ مثبت مع خيار «Add python.exe to PATH».
- ملاحظة: إن ظهر تحذير SmartScreen الأزرق (لأن الملف قادم من الإنترنت): More info → Run anyway.

### ملاحظة مهمة: خطأ Telegram Conflict
`Conflict: terminated by other getUpdates request` يعني أن نسختين من البوت تعملان بنفس التوكن معاً (تلجرام يسمح بواحدة فقط). `start_bot.bat` يعالج هذا تلقائياً:
- **قبل كل تشغيل**: يقتل أي نسخة بوت متبقية (python + bot.py) وينتظر 4 ثوانٍ
- **Ctrl+C أصبح إيقافاً حقيقياً**: البوت يتوقف نهائياً — إعادة التشغيل التلقائية تحدث فقط عند الانهيار الفعلي (كود خروج غير صفري)
- عند نقل المشروع أو تحديثه: أغلق كل النوافذ ثم شغّل السكربت — سينظف بنفسه
- إن شغّلت نفس التوكن من جهاز/سيرفر آخر مع هذا الجهاز فسيظهر الخطأ ذاته — توكن واحد لمكان تشغيل واحد

---

## 15) الجولة السادسة [R6] — واجهة 55bets الأصلية

### ما الذي تغيّر
استُبدلت القائمة الرئيسية القديمة بواجهة 55bets الأصلية **حرفياً** (الأزرار، المسميات،
النصوص، الترتيب، نمط العرض)، مع تشغيل كل ميزات الجولات 1-5 خلفها:

- **القائمة الرئيسية (14 زراً)**: الدخول ل55bets (رابط دخول تلقائي ببيانات المستخدم
  إن أنشأ حساباً عبر البوت)، 55bets حساب، شحن رصيد البوت، سحب رصيد من البوت، جوائز،
  إحالات، معلومات ملفي، السجل، كود هدية، تواصل معنا، شاركنا سعادتك، الشروحات (6 أقسام
  بنصوصها الأصلية)، عروضنا الحالية (من إعلانات اللوحة)، الشروط والأحكام.
  - الأدمن يرى زراً إضافياً: «👑 لوحة تحكم الأدمن».
  - صف الروابط (قناة/موقع/دعم) يبقى قابلاً للضبط من اللوحة.
- **الترحيب الحرفي**: «مرحبا,كيف يمكنني مساعدتك اليوم :» في /start والرجوع وتغيير اللغة.
- **الشحن الجديد**: طريقة الدفع (شام/سيرياتيل كاش مع رقم محفظة من اللوحة) ← المبلغ ←
  **رقم العملية (TXID)** ← الطلب. الطريقة وTXID محفوظان بعمود `note` الجديد وتصلهما
  إشعارات الفريق المالي وكارت الطلب.
- **السحب الجديد**: الطريقة ← **حساب الاستلام** (أو الدفتر كما في السابق) ← المبلغ.
  الوجهة تنتقل تلقائياً إلى أمر الدفع (payouts.destination) عند الاعتماد.
- **سجل العمليات** بنمط العرض الأصلي: النوع (الطريقة) | المبلغ | التفاصيل | التاريخ.
- **شاركنا سعادتك**: صور الإصابات تُمرَّر للأدمن (+ غرفة مراقبة اختيارية بالإعداد
  `mirror_chat_id`).
- **لوحة الأدمن** من الزر الجديد: إدارة المستخدمين، إدارة البوت (تشغيل/إيقاف البوت،
  قبول شام/سيرياتيل، إحصاءات الـWebhook، إعدادات الإحالة، أرقام المحافظ)، إدارة شام كاش
  (حركة اليوم)، إدارة الشحن (البونص والعمولة)، خزنة الأدمن (KPI)، أرصدة المحافظ،
  الإحصاءات، تقرير مخصص، واللوحة الكاملة السابقة.

### أعمدة/إعدادات جديدة (تلقائية بالكامل)
- `finance_requests.note` — يُضاف تلقائياً عند أول تشغيل (SQLite) وبعد `migrate_to_postgres.py`
  (Postgres). لا تدخل يدوي مطلوب.
- إعدادات جديدة عبر اللوحة: `sham_wallet_number`، `syriatel_wallet_number`،
  `accept_sham`، `accept_syriatel`، `bot_active`، `mirror_chat_id` (اختياري).

### الترقية
1. أغلق البوت، انسخ ملفات الحزمة فوق القديمة (deploy.sh أو يدوياً).
2. شغّل — `init_db` يضيف العمود الجديد وحده. القوائم القديمة تختفي وتحل الواجهة الجديدة.
3. من اللوحة الجديدة اضبط رقمَي المحفظة ليظهرا للمستخدمين بشاشات الشحن.

---

## 16) مراجعة الجولة السادسة [R6-REV] — 12 تحسيناً

1. **تبديل اللغة يعمل ومن القائمة الرئيسية**: زر «🌐 English / العربية» في القائمة
   الرئيسية — يبدّل كل واجهة 55bets (القائمة، الترحيب، الشاشات، الشروحات) بين
   العربية والإنكليزية.
2. **حُذف «تشغيل/إيقاف البوت» المكرر** من اللوحة الكاملة — يبقى من «إدارة البوت».
3. **أزرار رجوع في كل الشاشات** (الحاسبة، التحويل، مطالبة رقم العملية… وزر الرجوع
   يلغي أي خطوة معلقة تلقائياً).
4. **كلمة المرور يختارها المستخدم** عند إنشاء حساب 55bets (6 أحرف على الأقل) بدل
   التوليد العشوائي، وتُعرض مباشرة في «معلومات ملفي» دون إخفاء، وحُذف زر
   «إظهار كلمة المرور».
5. **إضافة مشرف من قائمة**: «إضافة/تحديث مشرف» تعرض المستخدمين بمعلوماتهم
   (المعرف، الاسم، @تلجرام، حساب البوت، الرصيد) — اختر مباشرة ثم فصّل الصلاحيات.
   يبقى الإدخال اليدوي بـID زراً مستقلاً.
6. **«إخفاء الأرصدة» يتفاعل**: الزر بالإعدادات يعرض «✅ مفعّل / ❌ معطل» ويُحدّث
   الشاشة فوراً عند التبديل.
7. **قائمة المستخدمين** تعرض اسم حساب البوت (site account) بجانب معلومات كل مستخدم.
8. **الترحيب باسم المستخدم**: «مرحباً {الاسم} 👋، كيف يمكنني مساعدتك اليوم :».
9. **«تذاكري» انتقلت** من «تواصل معنا» إلى «معلومات ملفي».
10. **إنشاء حساب لمستخدم (للوحة)**: بمعرف تلجرام @username بدل الرقم — يجب أن
    يكون المستخدم قد فتح البوت سابقاً (يُقبل الرقم احتياطياً).
11. **حظر/فك الحظر**: باسم حساب البوت الذي أنشأه المستخدم (يُقبل الرقم احتياطياً).
12. **سجل العمليات** يُعرض باللغتين حسب إعداد المستخدم.

ملاحظات فنية: لا أعمدة جديدة بالقاعدة — البحث بـ@username يستخدم عمود username
المخزّن تلقائياً عند كل /start. الترقية: استبدل الملفات وشغّل — لا خطوة يدوية.

16. **إدارة القناة/المجموعة والنشر المجدول [R6-CH]** + إصلاح /my_info:
    - **إصلاح /my_info**: أمر «معلومات ملفي» من قائمة الأوامر كان يُبتلع
      بصمت إذا كان المستخدم داخل خطوة إدخال معلقة — صار مسجّلاً مبكراً
      (أولوية على حالات الإدخال) ويفتح صفحة معلومات ملفي مباشرة.
    - **قائمة الأوامر الرسمية**: تُضبط تلقائياً عند كل إقلاع (start/my_info/
      cancel وللأدمن /now) — ما يظهر في زر القائمة يطابق البوت فعلياً.
    - **زر تبديل اللغة 🌐 صار في القائمة الرئيسية فقط** (أُزيل من معلومات
      ملفي).
    - **📣 نشر القنوات والمجموعات** (لوحة الأدمن ← إدارة البوت): عيّن قناة
      أو مجموعة (البوت مشرف فيها)، اختبر الوصول بضغطة، وأنشئ جدولات نشر:
      «آخر إعلان فعّال» كل X ساعة، نص مخصص (كل X ساعة أو نشر واحد فوري)،
      أو «تقرير يومي» وقت ثابت (شحن/سحب/معلقات). لكل جدولة: تفعيل/تعطيل،
      «▶️ انشر الآن»، حذف — والنشر يتحقق تلقائياً كل 10 دقائق.

15. **حزمة تحسينات [R6-PLUS]** — 9 ميزات جديدة:
    - **صورة إيصال الشحن**: بعد إدخال رقم العملية يمكن للمستخدم إرسال صورة
      الإيصال — تُمرَّر فوراً للفريق المالي مع رقم الطلب (اختيارية عبر زر
      «✅ إتمام الطلب بدون صورة»).
    - **المبالغ السريعة بشاشة الشحن**: أزرار ＋10/＋25/＋50/＋100 أعادت
      الظهور بشاشتي شام/سيرياتيل كاش.
    - **عمولة السحب**: نسبة قابلة للضبط من «إدارة الشحن والعروض» (0 افتراضياً)
      — تُخصم من مبلغ السحب ويظهر للمستخدم والفريق المالي الصافي والعمولة.
    - **تهدئة السحب**: مدة بالدقائق تمنع طلب سحب جديد قبل انتهائها من
      السحب السابق (0 = تعطيل) — تعرض للمستخدم الوقت المتبقي.
    - **شارة انتهاء العروض**: كل إعلان بشاشة العروض يعرض «⏳ ينتهي بعد X يوم»
      أو «⏳ آخر يوم اليوم!».
    - **آخر 3 عمليات** في صفحة «معلومات ملفي» مباشرة.
    - **إشعار تعديل الرصيد**: المستخدم يصله إشعار فوري عند أي تعديل إداري
      لرصيده (يُعطل بالإعداد adjust_notify=0).
    - **تنبيه الانهيار**: إن توقف التشغيل السابق فجأة يصلك «⚠️ تنبيه تشغيل»
      عند إعادة الإقلاع.
    - **أمر /now** (للأدمن): نبض لحظي — إجمالي الأرصدة، المعلقات، شحن/سحب
      اليوم، مدة التشغيل.
    - ملاحظة: سجل التدقيق (audit) كان موجوداً بالأدوات «📜 سجل التدقيق».

14. **زر رجوع في كل نوافذ إدخال الأدمن**: أي شاشة تطلب إرسال أمر تعديل
    (الولاء، الحضور، الحدود، العمولة، النصوص، الميزانية، الرمز، السحب التلقائي،
    الإعلانات، المسابقة، قوالب الرد، حظر/إنشاء مستخدم، أكواد الهدايا، إلخ —
    47 نافذة) صار بها زر «🔙 رجوع» يعود للقسم الأم ويلغي الإدخال المعلق
    تلقائياً. تعديل الرصيد من كارت المستخدم يعود للكارت نفسه.

13. **تعديل رصيد مستخدم من قائمة** (الشحن والسحب ← تعديل رصيد مستخدم): تعرض
    قائمة المستخدمين بالاسم و@تلجرام واسم حساب البوت والرصيد الحالي — اختر
    مباشرة فتنقلك لخطوة المبلغ مع بطاقة المستخدم (يبقى الإدخال اليدوي بـID
    زراً مستقلاً)، والرصيد يُخفى بـ••• للمشرف حين «إخفاء الأرصدة» مفعّل.

---

## 17) جولة الاقتراحات الثامنة عشرة [R6-PLUS2]

جولة شاملة نفّذت **17 اقتراحاً من 18** (الاقتراح 12 «نوافذ صيانة مجدولة»
كان موجوداً سلفاً [UI-4] فسقط من التنفيذ)، مع تعزيز ميزتين قائمتين:

### للأدمن
1. **كشف/حجب الوجهات المكررة** — عند طلب سحب تُطابق وجهة (محفظة/حساب) وجهة
   مستخدم آخر يُنبَّه فريق المالية تلقائياً؛ ومن «🧰 إدارة البوت ← 🕵️
   مطابقات الوجهات» قائمة المستخدمين ذوي الوجهات المشتركة، وزر
   «🚫 حجب/سماح» لكل مستخدم (`dest_block`) يمنع تكرار الوجهة قبل الاعتماد.
2. **تفصيل الملخص اليومي بالطرق** — التقرير اليومي المُرسل للقناة/الأدمن
   يتضمن الآن تفكيك الشحنات والسحوبات حسب طريقة الدفع (من حقل `method=`
   بملاحظات الطلبات).
3. **صلاحيات مالية دقيقة** — صلاحيتان جديدتان للمشرفين: «📥 الشحنات فقط»
   و«📤 السحوبات فقط» (إضافة لـ«💳 المالية كاملة»)؛ اعتماد/رفض الطلبات
   يُطبّق الصلاحية الدقيقة حسب نوع الطلب، وملاحظة الرفض تصل للمشرفين
   ذوي صلاحية نوع الطلب فقط. ومن شاشة المشرفين: «📊 نشاط المشرفين»
   (تقرير شهري لاعتمادات ورفوض كل مشرف مالي).
4. **سقف سحب خاص لكل مستخدم** — «🎚 سقف سحب خاص» بكارت المستخدم يضبط
   سقفاً يومياً خاصاً يتجاوز السقف العام (`wd_daily_cap`، 0 = تعطيل).
5. **مرآة النسخة الاحتياطية** — «📧 مرآة النسخ الاحتياطي» بإدارة البوت:
   chat_id قناة إضافية تُرسل إليها نسخة الأرشيف اليومية بجانب الأدمن.
6. **تذكير الطلبات العالقة** — «⏰ تذكير الطلبات العالقة»: إشعار متكرر
   لفريق المالية بكل طلب معلق تجاوز المدة المحددة (`pending_alert_hours`
   افتراضياً 6 ساعات، قابل للضبط من /settings) مع تهدئة 12 ساعة بين
   التذكيرات لكل طلب، وزر «⏰ ضبط ساعات التنبيه» (0 = إيقاف).
7. **رفض بسبب يصل للمستخدم** — زر «❌ رفض» يفتح خطوة سبب اختيارية
   (أو «🚫 رفض بدون سبب»)؛ يُرفق السبب برسالة الرفض التي تصله ويُحفظ
   بالسجل.

### للبوت
8. **بادج العروض** — نقطة حمراء 🔴 على زر «🍀 عروضنا الحالية» وجود
   إعلان فعّال لم يقرأه المستخدم بعد (جدول `announcement_reads`).
9. **إعلان بصورة** — خطوة اختيارية بعد نص الإعلان: أرسل صورة لتُنشر
   معه (تُعرض بالعروض وتُنشر على القناة بالمجدول و«انشر الآن»).
10. **اللغة الروسية** — زر اللغة بالقائمة أصبح دورة ثلاثية
    ar → en → ru مع ترجمة كاملة للقوائم والترحيب (باقي الشاشات
    تتعامل تلقائياً عبر المفاتيح المترجمة أو الإنجليزية بديلاً).
11. **إخفاء ذاتي للرصيد** — «🙈 إخفاء رصيدي» بصفحة ملفي يُخفي رصيد
    المستخدم بـ•••• في واجهاته (وليس أرصدة الآخرين).

### للمستخدم
13. **نقاط الولاء** — كل 1$ شحن معتمد = نقطة (`points_per_unit` من
    /settings)؛ من «🏪 معلومات ملفي ← ⭐ استبدال النقاط» يستبدلها
    برصيد مباشر (500⭐→5$، 1000⭐→10$، 2500⭐→25$) بخصم ذري.
14. **صدارة الإحالات للمستخدم** — «👥 احالات ← 🏆 الصدارة»: أفضل 10
    مُحيلين بعدد الإحالات مع وسام وأيقونة وترتيبك الشخصي.
15. **الحاسبة العكسية** — «🧮 حاسبة الشحن ← 🔄 عكسية: أريد صافي»:
    أرسل الصافي الذي تبيده ويحسب لك المبلغ المطلوب شحنه بعمولتك.
16. **رسالة رجوع للغائبين** — «🎁 رسالة الرجوع للغائبين» بإدارة البوت:
    تُفعل بجلسة مجدولة دورية لمن تجاوز `winback_days` (افتراض 14)
    دون إيداع، برسالة قابلة للتخصيص (`winback_msg`) ومرة واحدة
    لكل مستخدم (`last_winback`).
17. **فلاتر «طلباتي»** — أزرار «📋 الكل / 🗓 أسبوع / 📆 شهر» أعلى
    قائمة طلباتك المالية.
18. **تقييم ⭐ بعد السحب** — عند تعليم أمر الدفع «مدفوعة» يستلم
    المستخدم أزرار تقييم 1-5⭐، والمتوسط الشهري يظهر للأدمن في /now.

### قاعدة البيانات والاختبارات
- أعمدة جديدة: `users(wd_daily_cap, points, hide_balance, last_winback)`،
  `announcements(photo_file_id)`، `finance_requests(remind_at)`.
- جدول جديد: `payout_ratings(payout_id PK, telegram_id, stars, created_at)`.
- إعدادات جديدة: `points_per_unit(1.0)`، `dest_block`، `mirror_chat_id`،
  `pending_alert_hours(6.0)`، `winback_enabled(0)`، `winback_days(14.0)`،
  `winback_msg`.
- `migrate_to_postgres.py` حُدِّث ليشمل الأعمدة والجدول الجديدة.
- الاختبارات: `tests/test_r6plus2.py` — 16 فحصاً جديداً (المجموع 46:
  7 أساسية + 23 واجهة R6-REV + 16 جولة الاقتراحات) كلها خضراء.

---

## 18) جولة التحسينات [R6-PLUS3] — 14 اقتراحاً + مركز التفعيل + رمز حساب شام كاش

### 🎛 مركز تفعيل الميزات (جديد)
«⚙️ إعدادات البوت ← 🎛 تفعيل الميزات»: شاشة واحدة فيها **17 ميزة** كل منها
بزر ✅/❌ يبدّل التفعيل فوراً (تُحفظ بمفاتيح `feat_*` مع سجل تدقيق):

الملخص اليومي · التقرير الأسبوعي · تذكير العالقة · رسالة الرجوع للغائبين ·
نقاط الولاء · تقييم السحب ⭐ · بادج العروض 🔴 · بث الإعلان للجرس 🔔 ·
إشعار مستخدم جديد 🆕 · KPI الأرباح · KPI زمن المعالجة · الملخص الأسبوعي
الشخصي · تذكر الطريقة المفضلة · تأكيد السحب · الحاسبة العكسية · تنبيه
الوجهات المكررة · صيانة قاعدة البيانات.

### للأدمن
1. **لوحة الأرباح الصافية** — عمولات الشحن + رسوم السحب − مكافآت
   الإيداع − قيمة النقاط المستردة، لليوم و30 يوماً؛ تُلحق بـ/now
   (زر تفعيل مستقل).
2. **بث مقسّم للشرائح** — شريحتان جديدتان بجانب الموجود: 🇷🇺 الروسية
   فقط، و💤 الغائبون 14+ يوماً (بلا إيداع منذ أسبوعين).
3. **حظر مؤقت بمُدة** — بكارت المستخدم «⏳ حظر مؤقت»: 1-365 يوماً مع
   فك تلقائي عند انتهاء المدة وعرض المتبقي بالكارت (0 = إلغاء).
4. **إشعار مستخدم جديد** — رسالة فورية للأدمن بالاسم والمعرف
   (افتراضياً معطلة — تُفعّل من المركز).
5. **KPI زمن المعالجة** — متوسط ساعات اعتماد الشحن/السحب آخر 7 أيام
   في /now.
6. **شاشة «⭐ آخر التقييمات»** — آخر 20 تقييماً مع المبالغ ومتوسط
   7 أيام (إدارة البوت)، وسطر تقييم اليوم بالملخص اليومي.

### للمستخدم
7. **ملخص شخصي أسبوعي** — كل أحد: شحناته وسحوباته ونقاطه وترتيبه
   بالإحالات؛ يفعّله من «🏪 معلومات ملفي ← 📭 ملخصي الأسبوعي»
   (وبوابة عامة بالمركز).
8. **تأكيد السحب بالصافي** — بطاقة قبل الإرسال: المبلغ، العمولة،
   الصافي، الوجهة، والرصيد بعد السحب، بزر تأكيد/إلغاء.
9. **تذكر الطريقة المفضلة** — آخر طريقة شحن/سحب تتصدّر الأزرار تلقائياً.
10. **سجل النقاط** — «⭐ استبدال النقاط ← 📜 سجل نقاطي»: آخر 15 حركة
    كسب/استبدال (يُسجّل الكسب الآن تلقائياً مع كل شحن معتمد).
11. **جرس العروض 🔔** — من شاشة العروض يشترك ليستلم كل إعلان جديد
    فور نشره (حتى 300 مشترك لكل إعلان).

### تقنية
12. **صيانة قاعدة البيانات** — `PRAGMA optimize` يومي + `VACUUM`
    وفحص سلامة أسبوعي (السبت) مع تنبيه الأدمن إن لم تكن النتيجة ok.
14. **فهارس جديدة** — `finance_requests(created_at)`،
    `transactions(created_at)`، `users(referrer_id)`،
    `payouts(created_at)` — تسرّع الفلاتر والتقارير.
15. **أمر /ops** — تشخيص فوري: الإصدار، حجم القاعدة وسلامتها، آخر
    نسخة احتياطية، المعلّق، المنشورات المجدولة، الميزات المفعّلة،
    مدة التشغيل.

### 💠 شام كاش — رمز الحساب
- طريقة شحن ثالثة «شام كاش — رمز حساب» تظهر للمستخدمين بجانب المحفظة
  وسيرياتيل، بنص مخصص يعرض **رمز الحساب**.
- تعديل الرمز: «⚙️ إدارة شام كاش ← 🔣 رمز حساب شام كاش» (يُحفظ
  بمفتاح `sham_account_code` ويسجل بالتدقيق).
- تُسجّل الطلبات بطريقة «شام كاش (رمز حساب)» فيفصّلها الملخص اليومي
  عن المحفظة.

### قاعدة البيانات والاختبارات
- أعمدة users الجديدة: `ban_until, digest_enabled, ann_notify,
  last_dep_method, last_wd_method` (+ مزامنة migrate_to_postgres).
- `tests/test_r6plus3.py` — 18 فحصاً جديداً (المجموع 64: 7+23+16+18).
- إصدار البوت يظهر الآن في /ops: `5.3 (R6-PLUS3)`.

---

## 19) جولة التعديلات [R6-PLUS4]

### 💠 شام كاش — دمج طريقتي الشحن في زر واحد
- زر «شام كاش (Sham Cash)» واحد بقائمة الشحن (أُزيل الزر المنفصل
  لرمز الحساب).
- **الأدمن يحدد ما يراه المستخدم**: «⚙️ إدارة شام كاش ← 💳/🔣 يظهر
  للمستخدم: رقم المحفظة / رمز الحساب» — زر تبديل فوري (مفتاح
  `sham_code_mode`)، وقيمة الرمز تُعدّل من «🔣 رمز حساب شام كاش».
- الطلبات تُسجّل بطريقة «شام كاش» موحدة.

### 🌐 الواجهة الرئيسية بلغتين
- زر اللغة يتبدل بين **العربية والإنكليزية فقط** (أُزيلت الروسية
  من الدورة؛ المستخدم المضبوط على الروسية يبقى على نصوصها).

### 👥 إحالاتي — خيارا العدد والرابط
- زر ثابت «👥 عدد إحالاتي: N» أعلى شاشة الإحالات (يظهر حتى بلا
  إحالات).
- زر جديد «🔗 رابط الإحالة الخاص بي»: يعرض الرابط الشخصي
  `t.me/<البوت>?start=ref_<معرفك>` مع زر مشاركة — **هو نفسه رابط
  نظام الإحالات القائم** (`/start ref_`): يُحتسب للمستخدم تلقائياً
  بمجرد دخول صديق عبره، مع مكافأة الإحالة والإشعار المعتادين.

### 🐛 إصلاح إنتاجي
- سدادة الصدارة: تخزين الإحالة في `/start` يتم بـtelegram_id بينما
  كان استعلام لوحة الصدارة يربط على المفتاح الداخلي — أُوحّد الربط
  (كان سيعرض أسماء خاطئة عند حدوث فرق بين المعرفين).

ملاحظة: فكرة «زر رصيدي في الموقع» سُحبت بطلب المستخدم — شاشة
«معلومات ملفي» بلا تغيير في هذا البند.

### 🐛 إصلاح عاجل [R6-PLUS4F]: زر اللوحة للمشرفين
**الأعراض**: بعد إضافة مشرف ومنحه «🌐 كل الصلاحيات» ظل البوت يظهر له
كمستخدم عادي بلا أي مدخل للوحة.

**السبب**: الصلاحيات نفسها كانت تعمل (`/admin` ومكالمات
`is_admin_or_supervisor`)، لكن زر «👑 لوحة تحكم الأدمن» في القائمة
الرئيسية كان مقيّداً بـ«الأدمن الرئيسي فقط» (`telegram_id ==
ADMIN_USER_ID`) في موضعين — فالمشرف لا يرى أي مدخل مرئي.

**الإصلاح**: زر اللوحة يظهر الآن لكل من يجتاز
`is_admin_or_supervisor()` (الأدمن + المشرف غير الموقوف وغير
المحظور، مع بقاء انقطاع الزر أثناء إيقاف البوت سلوكاً مقصوداً).
تحقق: `tests/test_r6plus3.py` — فحص جديد (المجموع 67).

---

## 20) نقل/تحديث البوت بدون فقد بيانات تسجيل الدخول

بياناتك كلها في **3 ملفات** بجانب البوت (لا شيء منها داخل الحزمة):
1. `bot.db` — المستخدمون، أسماء/كلمات حسابات الموقع، الأرصدة، الطلبات.
2. `secret.key` — مفتاح تشفير كلمات مرور الموقع. **فقدانه = استحالة
   فك أي كلمة مرور محفوظة** حتى مع وجود bot.db.
3. `.env` — التوكن والإعدادات (إن كنت تستخدم `PASSWORD_ENCRYPTION_KEY`
   بدل الملف فهو بديل secret.key).

### أ) تحديث على نفس الجهاز
1. أوقف البوت (أغلق نافذة start_bot_fast.bat / `systemctl stop bot`).
2. نسخة أمان سريعة: انسخ `bot.db` و`secret.key` و`.env` لمجلد خارجي.
3. فك محتويات الحزمة الجديدة فوق المجلد (استبدال الملفات) — الحزمة
   لا تحتوي أي ملف من الثلاثة فلا تُمس بياناتك.
4. شغّل البوت — عند أول إقلاع تُضاف الأعمدة/الجداول الجديدة تلقائياً
   (ALTER محمي) دون أي مساس بالبيانات القديمة.

بديل آلي على لينكس: `bash deploy.sh bot_project_v5.zip` — يأخذ نسخة
احتياطية من bot.db وsecret.key و.env قبل التحديث ولا يلمسها أصلاً.

### ب) الانتقال لجهاز جديد
1. ثبّت بايثون + المكتبات (start_bot.bat مرة واحدة).
2. انقل الملفات الثلاثة **معاً** إلى المجلد الجديد **قبل أول تشغيل**:
   `bot.db` (+ `bot.db-wal` و`bot.db-shm` إن وُجدا وأنت متوقف)
   و`secret.key` و`.env`.
3. شغّل — سيكمل البوت بكل بياناته وكلمات المرور تعمل.

### ملاحظات
- لا تنسخ bot.db والبوت شغّال (ملفات WAL ناقصة) — أوقفه أولاً.
- زر «🗄 نسخة احتياطية الآن» يرسل لك نسخة bot.db على تلجرام — تستصلحها
  بوضعها مكان bot.db وأنت متوقف.

---

## 21) جولة الإضافات [R6-PLUS5] — 7 ميزات

### 👑 للوحة التحكم
- **C5 🔢 حد سحب يومي بالعدد** — «⚖️ حدود وسقوف ← حد سحب يومي»:
  أقصى عدد طلبات سحب/يوم لكل مستخدم (0 = غير محدود)؛ التجاوز
  يرفض قبل الإنشاء برسالة واضحة.
- **B4 🕐 ساعات عمل الدعم** — «⚙️ إعدادات البوت ← 🕐 ساعات عمل
  الدعم» بصيغة `09:00-23:00` UTC (أو «مسح» = دائماً)؛ أي تذكرة
  تُفتح خارج الأوقات يستلم صاحبها تنويهاً لطيفاً بوقت التواجد.
- **B5 ✍️ توقيع مشرف الدعم** — ردود التذاكر تُختتم تلقائياً بـ
  «— دعم {الاسم}» (الاسم من تلجرام)، + زر «📊 ردود الدعم» بالطابور
  الموحد يعرض عدد ردود كل مشرف آخر 30 يوماً.

### 🔒 مالي
- **C1 احتجاز رصيد السحب المعلق** (ميزة قابلة للتفعيل «🔒 احتجاز
  رصيد السحب المعلق» بالمركز، مفعّلة افتراضياً) — لحظة إنشاء الطلب
  يُخصم الصافي فوراً (حركة `wd_hold` بسجل المستخدم)، والاعتماد لا
  يخصم مرتين، والرفض أو انتهاء الصلاحية يُعيد المبلغ (حركة
  `wd_refund`) — سُدّت ثغرة صرف طلبين على رصيد واحد.

### 👤 للمستخدمين
- **U15 🎂 هدية ذكرى الانضمام** (ميزة قابلة للتفعيل) — بعد 30
  يوماً: +100 نقطة، وكل سنة: +1000 نقطة (قابلة للضبط
  `anniv_month_points` / `anniv_year_points`) برسالة تهنئة
  وتسجيل بالسجل — مرة واحدة لكل ذكرى (`anniv_gifts`).
- **U16 ⚡ مبالغك السريعة** — من شاشة الشحن «⚙️ مبالغك السريعة»:
  حتى 4 مبالغ خاصة تحل محل 10/25/50/100 (حفظ/مسح/تحقق).
- **U17 🆘 مساعدة داخل التدفق** — زر «🆘 مساعدة» بشاشة الشحن وبطاقة
  تأكيد السحب يفتح تذكرة دعم فوراً بنسق «بخصوص الشحن/السحب».

### قاعدة البيانات والاختبارات
- أعمدة users: `quick_amounts, anniv_gifts` (+ مزامنة Postgres).
- ميزات بالمركز: 19 الآن (أُضيف anniv_gift وwd_hold).
- `tests/test_r6plus5.py` — 10 فحوصات (المجموع 77: 7+23+16+21+10).
- إصلاح اختبار سابق تعطّل بصمت: /now أصبح رسالتين منذ §18 واختبار
  §16 القديم فحص الأخيرة فقط — حُوّل لفحص كل الرسائل.

---

## 22) جولة الأمان والمراقبة [R6-PLUS6] — 4 ميزات

### 🛟 D3: نسخة أمان قبل ترقية الإصدار
البوت يحفظ رقم الإصدار بالقاعدة (`schema_version`). عند إقلاع إصدار
أحدث: ينشئ `bot_preupgrade_<وقت>.db` تلقائياً ويرسله للأدمن مع
تنبيه، ثم يحدّث الرقم — والإقلاعات التالية لنفس الإصدار لا تكرر.
**الرجوع**: أوقف البوت، استبدل bot.db بالنسخة، رجّع bot.py القديم.

### 🚨 D4: تنبيه «شحن ثم سحب فوري»
ميزة قابلة للتفعيل (مفعّلة) مع نافذة زمنية قابلة للضبط
`inst_dep_window_min` (افتراضي 30 دقيقة): من شحن ثم طلب سحب
(سحب فعلي أو احتجاز) خلال النافذة → تنبيه فوري للأدمن والمرآة
«راجع النمط — قد يكون تجربة أو غسلاً».

### 📵 D8: تنبيه انسداد البوت لدى مستخدم
حارس `notify_user_guarded` على الرسائل الدورية (الرجوع للغائبين +
هدية الذكرى): 3 إخفاقات متتالية (حظر البوت لدى المستخدم) → تنبيه
واحد للأدمن بمعرف المستخدم وتوصية بمراجعة كارته، مع تصفير العداد
عند نجاح الإرسال.

### 🕓 U24: طمأنة الطلب المعلق
داخل تذكير العالقة القائم: عند أول تذكير (6 ساعات افتراضياً)
يستلم صاحب الطلب رسالة «طلبك #N قيد المراجعة — شكراً لصبرك»
بلغتين، مع بقاء تذكير الفريق المالي كما هو.

### فحوصات وتنبيهات
- الإصدار: `5.4 (R6-PLUS6)` — ميزات المركز أصبحت 20.
- `tests/test_r6plus6.py` — 8 فحوصات (المجموع 85: 7+23+16+21+10+8).
- ملاحظة فنية: كشف «الفوري» يشمل حركة `wd_hold` لأنها تُنشأ بنفس
  ثانية الشحن أحياناً — فُعّل كسر التعادل `w.id > d.id` بجانب
  المقارنة الزمنية.

---

## 23) المراجعة الشاملة [AUDIT] — إصلاحات وثوابت

مراجعة كاملة لكل الميزات والخيارات (فحص ساكن آلي + مراجعة يدوية
للتدفقات الحرجة). النتيجة: **7 إصلاحات** مطبقة، وعدد من البنود
استُبعد كإنذارات كاذبة بعد التحقق. لا تغييرات على المخطط
(`SCHEMA_VERSION = 6` كما هو).

### إصلاحات العيوب المؤكدة

1. **F1 — حارس رصيد عند الاحتجاز (`wd_hold`)**: كان الخصم يتم
   بلا تحقق ثانٍ؛ لو تغيّر الرصيد بين عرض بطاقة التأكيد والضغط
   عليها (تحويل/خصم آخر) لصار الرصيد سالباً. الآن `UPDATE ...
   WHERE balance >= ?` — عند الفشل: تراجع كامل، إلغاء الطلب
   (`expired`)، رسالة «تغيّر رصيدك»، وتنظيف الحالة.
2. **F2 — تنبيه «شحن ثم سحب فوري» يتكرر كل ساعة**: مفتاح
   منع التكرار كان يحمل ساعة اللحظة `{inst_{uid}_{HH}}` فيُجدد
   كل ساعة. أصبح `inst_{uid}` — تنبيه واحد لكل نمط.
3. **F3 — هدية الذكرى بسقف 500 مستخدم**: الاستعلام `LIMIT 500`
   كان يمر على الأوائل فقط. أصبح تمرير بدفعات من 500 حتى
   نهاية الجدول (`id > last ORDER BY id`).
4. **F4 — `last_wd_method` يُكتب ولا يُقرأ**: المفضلة كانت
   تطبق على شاشة الشحن فقط وبمطابقة نصية هشة. أصبح مساعد
   `_reorder_kb_by_cb` يطابق `callback_data` (أدق) ويطبق على
   **شاشتي الشحن والسحب** معاً.
5. **F5 — ست إعدادات بلا واجهة ضبط**: `points_per_unit`,
   `winback_days`, `winback_msg`, `anniv_month_points`,
   `anniv_year_points`, `inst_dep_window_min` — أُضيفت شاشة
   «🔢 إعدادات القيم» بلوحة الأدمن (عرض القيم الحالية + الافتراضية،
   تعديل رقمي/نصي مع تحقق، تسجيل audit لكل تعديل).
6. **F6 — سياق استفسار الدعم يضيع**: زر «استفسار شحن/سحب»
   يعرض شرحاً ثم يفتح تذكرة عادية. الآن يُحفظ السياق
   (`flow_ctx`) وتتحول التذكرة إلى
   `🛒 [استفسار شحن] …` / `🏦 [استفسار سحب] …`.
7. **F7 — نسخة أمان على تثبيت فارغ**: الترقية كانت تصنع
   `bot_preupgrade_*.db` حتى لقاعدة فارغة (ضجيج بلا فائدة).
   الآن: قاعدة بلا مستخدمين → تحديث `schema_version` مباشرة
   بلا نسخة.

### استُبعد بعد التحقق (إنذارات كاذبة)

- مطابقة المفضلة النصية القديمة كانت مقصودة بأسماء الأزرار —
  استُبدلت بمطابقة callback_data لثبات أعلى (تحسين وليس عيباً).
- «أزرار بلا معالج»: 5 أزرار تُعالج عبر `F.data.in_`
  (مثل أزرار التذاكر) — مغطاة.
- إعدادات «لا تُكتب»: تُكتب عبر مسارات ديناميكية
  (`LIM_FIELDS`, `feat_{key}`, `{key}_wallet_number`) — سليمة.
- معالجا `DepositFSM.photo` ليسا تكراراً: الأول لـ`F.photo`
  والثاني لأي شيء آخر (تلميح).

### ثوابت معروفة (موثقة، غير مصلحة بهذه الجولة)

- `_alerted_ids` و`_FAIL_STREAK` ينموان بذاكرة العملية وتُمسح
  مع إعادة التشغيل — حجمها مهمل عملياً.
- الفهارس الأربعة لم تُضف لمهاجر PostgreSQL
  (`migrate_to_postgres.py`) — ترقية bot.py تعوضها على SQLite.
- سجل العمليات يعرض `wd_hold`/`wd_refund` دون شاشة مراجعة
  خاصة — سلوك مقبول حالياً.

### فحوصات وتنبيهات

- الإصدار: `5.5 (AUDIT)`.
- `tests/test_audit_fixes.py` — 8 فحوصات (المجموع **93**:
  7+23+16+21+10+8+8)، منها: منع الرصيد السالب عند الاحتجاز،
  عدم تكرار التنبيه الفوري، تغطية الذكرى، ترتيب المفضلة
  (شحن+سحب)، محرر القيم (صحيح/خاطئ)، سياق تذكرة الدعم،
  تخطي النسخة على التثبيت الفارغ، واكتمال مفاتيح r6t/STRINGS.
- تثبيت `test_r6plus5`: نافذة الدعم المغلقة صارت نسبية للوقت
  الحالي بدل افتراض ساعة UTC ثابتة (هشاشة زمنية).

---

## 24) جولة الاقتراحات الخامسة [R6-PLUS7] — 4 ميزات

البنود المعتمدة من د5: T1، U31، U32، E4. لا تغييرات على المخطط
(`SCHEMA_VERSION = 6`) — مركز الميزات أصبح **24 مفتاحاً**.

### 🧹 T1: تنظيف دوري لذاكرة التنبيهات
ميزة `mem_cleanup` (مفعّلة): مرة كل 24 ساعة تُفرّغ `_alerted_ids`
(مفاتيح منع تكرار التنبيهات) وتحذف عدادات الفشل الصفرية من
`_FAIL_STREAK` — فلا تنمو الذاكرة مع الأيام. مفاتيح اليوم تُستهلك
ذاتياً والتنظيف آمن بحكم التصميم (يعيد التنبيه فقط لنمط جديد
فعلي بعد يوم كامل). مسجلة ب`last_mem_cleanup`.

### ✅ U31: تحقق رقم وجهة السحب
ميزة `wd_dest_check` (مفعّلة): عند إدخال حساب الاستلام في السحب:
- **أرقام سورية سليمة** (09xxxxxxxx / 9xxxxxxxx / +963 /
  00963) → تُطبّع تلقائياً إلى صيغة موحدة `+9639XXXXXXXXX`
  وتُحفظ نظيفة بالطلب.
- **أرقام لا تشبه الصيغة السورية** → شاشة تحكيم: «⚠️ الرقم لا
  يبدو رقماً سورياً» + (✅ تأكيد / ✏️ تعديل) — لا يضيع إدخال
  المستخدم.
- **حروف أو طول غير منطقي** → رفض مع طلب إعادة الإدخال.
- إيقاف الميزة يعيد السلوك القديم حرفياً.

### 📊 U32: شاشة «الأسعار والحدود» للمستخدم
ميزة `rates_screen` (مفعّلة): زر «📊 الأسعار والحدود» بشاشة
الخدمات يعرض: نقاط الولاء لكل 1$، عمولة السحب، حدود الشحن
والسحب (من إعدادات حدود وسقوف الحية)، وحد السحوبات اليومي
(∞ إن لم يُضبط) — بالعربية والإنكليزية، تُقلل أسئلة الدعم.

### 🚧 E4: الصيانة المجدولة — إعلان مسبق + نطاق «مالية فقط»
بناء على الصيانة المجدولة القائمة (NEW 45):
- **نطاق الصيانة** (زر «🚧 نطاق الصيانة» بلوحة الإعدادات،
  للأدمن الرئيسي): «إيقاف كامل 🔴» (السلوك القديم — البوت
  يتوقف تماماً) أو «العمليات المالية فقط 🟡» — التنقل والدعم
  يعملان، ومنع الشحن والسحب خلال النافذة برسالة الصيانة مع
  وقت العودة. محفوظ ب`maintenance_scope`.
- **إعلان مسبق**: ميزة `maint_announce` (مفعّلة) + إعداد
  `maint_announce_min` (افتراضي 60 دقيقة، من شاشة «إعدادات
  القيم»): قبل بدء النافذة بهذه المدة يُبث إعلان «🔔 تنبيه
  صيانة مجدولة» لكل المستخدمين غير المحظورين بلغتهم — مرة
  واحدة يومياً (مسجل ب`last_maint_announce`).

### فحوصات وتنبيهات
- الإصدار: `5.6 (R6-PLUS7)` — الميزات 24، المعالجات 337 بلا
  تكرار.
- `tests/test_r6plus7.py` — 12 فحصاً (المجموع **105**:
  7+23+16+21+10+8+8+12).
- ملاحظة: بوابة منع المالية تعمل في النطاقين — في «الكل»
  يسبقها منع `user_allowed` العام، وفي «المالية فقط» هي
  الحاجز الفعلي أمام السحب والشحن.

---

## 25) كونسول نظيف + سجل أخطاء [R6-PLUS8]

استبدال معالج الأخطاء العام القديم (كان يطبع التتبع الكامل لكل
استثناء) بنسخة تصنيفية تُبقي الكونسول مقروءاً دون إخفاء أي
مشكلة حقيقية. ميزة `quiet_errors` (مفعّلة — مركز الميزات أصبح
**25 مفتاحاً**)؛ إيقافها يعيد السلوك القديم حرفياً (تتبع كامل
بالكونسول).

### التصنيف
| النوع | أمثلة | التعامل |
|-------|-------|---------|
| **بريء** | «query is too old» (ضغطة على رسالة قديمة)، «message is not modified»، حظر البوت لدى المستخدم، ضغط تليجرام RetryAfter | سطر INFO قصير `[تجاهل]` — بلا تتبع |
| **شبكة** | انقطاع لحظي/مهلة/ConnectionError | تحذير قصير `[شبكة]` — يُعاد تلقائياً |
| **جدي** | أي خطأ آخر (برمجي/تليجرام غير معروف) | سطر ERROR قصير بالكونسول + **التتبع الكامل** يُلحق بملف `error_log.txt` مع الوقت ومعرف المستخدم |

### لماذا يظهر «query is too old» أصلاً؟
تليجرام يمنح عمراً محدوداً للضغط على الأزرار الداخلية (~دقيقة).
الضغط على أزرار رسالة قديمة (خاصة قبل إعادة تشغيل البوت) يرفضه
السيرفر — لا تأثير له على عمل البوت، وكان يطبع تتبعاً طويلاً
بلا فائدة.

### ملاحظات تشغيلية
- `error_log.txt` يُنشأ بجوار البوت عند أول خطأ جدي فقط —
  لا يُنشأ مسبقاً ولا يمتد بلا أخطاء.
- نفس «التجمد اللحظي» الذي يعود بالضغط على Enter هو خاصية
  QuickEdit بنافذة ويندوز (نقرة ماوس داخل النافذة تحدد النص
  وتجمّد البرنامج مؤقتاً) — لا علاقة له بالبوت؛ يمكن تعطيلها
  من خصائص النافذة.

### فحوصات وتنبيهات
- الإصدار: `5.7 (R6-PLUS8)`.
- `tests/test_r6plus8.py` — 8 فحوصات (المجموع **113**:
  7+23+16+21+10+8+8+12+8): كل تصنيف + الملف + البوابة
  المعطلة + استخراج معرف المستخدم.

---

## 26) جولة الواجهة والتجربة [R6-PLUS9] — 14 تحسيناً (V1–V14)

جولة UI/UX كاملة: كل البنود طبقة عرض — **لا تغيير على أي منطق
مالي أو المخطط** (`SCHEMA_VERSION = 6`). مركز الميزات أصبح
**35 مفتاحاً** (10 جديدة، كلها قابلة للتفعيل/الإيقاف).

### للمستخدم
- **V1 🧭 مؤشر الخطوات** (`step_bars`، مفعّلة): «📍 الخطوة 2 من 4
  ▓░░░» في شحن (طريقة←مبلغ←رقم عملية←إيصال اختياري) وسحب
  (طريقة←حساب←مبلغ←تأكيد).
- **V2 💳 شريط الرصيد** (`bal_bar`): «رصيدك الحالي» أعلى شاشات
  إدخال الشحن والسحب.
- **V3 👋 ترحيب ذكي** (`smart_welcome`): «صباح/مساء الخير» حسب
  وقت **دمشق** (UTC+3) + اسم المستخدم — بدل النص الجامد.
- **V4 🎓 جولة تعريفية** (`onboarding`): 3 شرائح تفاعلية لكل
  مستخدم جديد عند /start (شحن/سحب/نصائح) مع تتبع من أكملها
  (`tour_done_{tid}`) وزر تخطٍّ.
- **V5 ✅ سطر طمأنة** (`clear_receipt`): «سيصلك إشعار فور
  الاعتماد — لا داعي لإعادة الإرسال» يُلحق بإيصالي الشحن والسحب.
- **V6 🕐 توقيت دمشق** (`damascus_time`، **معطلة** افتراضياً):
  عند تفعيلها تُعرض كل تواريخ سجل المستخدم وكارت الأدمن
  بتوقيت سوريا +«دمشق» بدل UTC.
- **V7 ❌ إلغاء صريح**: زر «إلغاء» بكل شاشات الإدخال (حساب
  السحب، مبلغ السحب، رقم العملية) ينظّف الجلسة ويعيد القائمة —
  بلا حاجة لكتابة /cancel.
- **V8 🖼 ترحيب بصورة** (`welcome_photo`، معطلة): ارفع صورة من
  «⚙️ الإعدادات ← 🖼 ترحيب بصورة» تصبح شعار /start مع الترحيب
  نصياً كتعليق؛ «مسح» يزيلها.
- **V13 🇬🇧 أزرار بلغتين**: زر حاسبة الشحن صار
  «🧮 حاسبة الشحن / Deposit calculator» — والأزرار الجديدة كلها
  ثنائية اللغة منذ البداية.
- **V14 🗂 سجل مختصر/مفصل**: زر تبديل بشاشة السجل — المختصر
  سطر واحد لكل عملية (النوع، المبلغ، وقت دمشق إن فُعل)،
  والمفصل كما كان.

### للأدمن
- **V9 👤 زر الملف بالإشعارات**: إشعارات الفريق المالي كانت
  تحتوي أصلاً ✅/❌ — أُضيف «👤 الملف» يفتح كارت صاحب الطلب
  مباشرة من الإشعار (`fcard:{rid}`).
- **V10 🔢 عدادات حية** (`live_counters`): «🧾 الطابور الموحد (n)»
  و«💳 المالية (n)» تُحسب لحظة فتح اللوحة.
- **V11 🔙 رجوع للسابقة**: البوت يتذكر آخر شاشة إدارية زيّرها
  (المستخدمون/الطابور/KPI/الإحصائيات/قوائم المستخدمين) ويعرض
  بزر «🔙 رجوع للسابقة» أعلى كارت المستخدم — بدل رجوع ثابت.
- **V12 📈 رسم الأرباح** (`profit_chart`): زر بـKPI الحي يرسل PNG
  بأعمدة صافي الربح اليومي لآخر 30 يوماً (أخضر/أحمر) مع
  الإجمالي — نفس معادلة §19.

### فحوصات وتنبيهات
- الإصدار: `5.8 (R6-PLUS9)` — 344 معالجاً بلا تكرار فعلي
  (فحص الازدواج يرى زوج DepositFSM.photo المعروف كإنذار كاذب).
- `tests/test_r6plus9.py` — 17 فحصاً (المجموع **130**:
  7+23+16+21+10+8+8+12+8+17).
- تحديث فحص قديم: `test_r6rev` يقبل الآن التحيات الثلاث
  (مرحباً/صباح/مساء) لاعتماد الترحيب الذكي.

---

## 27) جولة د7 [R6-PLUS10] — 12 بنداً (حماية + تحليلات + مستخدم)

المبنود المعتمدة من دفعة الاقتراحات السابعة كاملة. لا تغييرات
على المخطط (`SCHEMA_VERSION = 6`) — مركز الميزات أصبح
**46 مفتاحاً** (11 جديداً؛ تنظيف النسخ يعمل ضمن مسار النسخ
القائم بلا ميزة).

### 🛡 حماية مالية
- **F8 ⛔ منع تكرار رقم العملية** (`txid_dedup`، مفعّلة): نفس
  TXID بنفس الطريقة لا يُقبل مرتين (معلق أو معتمد) — رسالة
  «مسجّل سلفاً» وتنظيف الجلسة. طرق مختلفة بنفس الرقم تُقبل
  (كله محسوم بالطريقة).
- **F9 ⏰ الاحتجاز العالق** (`hold_stale`، مفعّلة): طلب سحب
  محتجز (`held=1`) بقي دون قرار أكثر من `hold_stale_hours`
  (افتراضي 12، من «إعدادات القيم») → تنبيه للفريق المالي مرة
  واحدة لكل طلب مع عمره ومبلغه.

### 📊 تحليلات
- **E7 ♻️ الاحتفاظ الأسبوعي** (`retention`، مفعّلة): كل أحد
  بعد 9UTC — من شحن هذا الأسبوع (جدد مقابل عائدون + نسبة
  العودة) للأدمن والمرآة، ومسجّل ب`last_retention_week`.
- **E8 💎 LTV بكارت المستخدم** (`user_ltv`): «شحناته | سحبه |
  صافيه» منذ التسجيل — تظهر مباشرة تحت الرصيد.
- **E9 🔥 ساعات الذروة** (`peak_hours`): أعلى 5 ساعات نشاطاً
  (آخر 7 أيام) بنهاية شاشة KPI.
- **E11 👁 معاينة التقرير الأسبوعي** (`weekly_now`): زر جديد
  بشاشة التقارير يعرض ملخص آخر 7 أيام + الاحتفاظ فوراً —
  دون انتظار يوم الأحد (لا يمس المجدول القائم).

### 👤 للمستخدم
- **U35 📍 آخر طلبي** (`last_request`): زر أعلى شاشة السجل
  يعرض آخر طلب: النوع والمبلغ والحالة ووقته — مع تنويه «رصيدك
  محتجز لحين القرار» لطلبات الاحتجاز.
- **U36 🔔 تنبيه نزول الرصيد** (`bal_alert`): من صفحة «معلوماتي»
  يضبط المستخدم حداً (0 للإلغاء)؛ حين ينزل رصيده تحته يصله
  إشعار مرة واحدة، ويعاد تسليحه تلقائياً حين يرتفع فوق الحد.
- **U37 ⭐ مبلغ شحن مفضل** (`fav_amount`): زر ⭐ بمبلغه المعتاد
  يتصدر قائمة المبالغ السريعة بشاشة الشحن (0 يحذفه).
- **U38 🔄 إعادة المحاولة** (`retry_btn`): رفض طلب شحن يصل
  للمستخدم مع زر «🔄 أعد المحاولة» يفتح شاشة الشحن مباشرة.

### 🛠 تشغيل
- **E10 🧹 تنظيف نسخ الترقية**: بعد كل نسخة أمان جديدة تُحذف
  الأقدم ويُبقى آخر 3 (`bot_preupgrade_*.db`) — بلا تراكم.
- **E12 📝 أسباب رفض جاهزة** (`reject_reasons`): شاشة الرفض
  أصبحت أزرار: «رقم العملية غير صحيح / الحساب غير مطابق /
  المبلغ غير واصل / سبب آخر (اكتب) / بدون سبب» — الجاهزة
  تُرسل للمستخدم بصيغة مهذبة بضغطة واحدة.

### فحوصات وتنبيهات
- الإصدار: `5.9 (R6-PLUS10)` — 353 معالجاً بلا تكرار فعلي.
- `tests/test_r6plus10.py` — 15 فحصاً (المجموع **145**:
  7+23+16+21+10+8+8+12+8+17+15).
- ملاحظة أمان: فحص F8 لا يمنع المستخدم نفسه من إعادة المحاولة
  برقم جديد — يمنع إعادة استخدام نفس الرقم فقط.

---

## 28) الفحص الشامل الثاني [AUDIT2]

فحص كامل لكل الكود بعد جولات PLUS7–PLUS10 (ساكن آلي: بوابات
الميزات، تغطية الأزرار، مسارات الإعدادات، تكرار المعالجات +
مراجعة يدوية). النتيجة: **5 عيوب صُلحت**، ولا تغييرات على
المخطط (`SCHEMA_VERSION = 6`).

### العيوب المصلحة
1. **زر «مختصر/مفصل» كان ميتاً**: زر تبديل عرض السجل (V14)
   كان يُصدر `hist_toggle:` **بلا معالج** — الضغط عليه لا
   يفعل شيئاً. أُضيف المعالج الذي يعيد رسم السجل بالوضع
   المعاكس.
2. **F8 مطابقة جزئية لرقم العملية**: فحص منع التكرار كان
   `instr(note, 'txid=X')` — رقم `TX001` كان يمنع `TX001234`
   خطأً. صار بمحددات `txid=X;` — مطابقة تامة فقط.
3. **زر «الأسعار والحدود» يظهر مع الميزة معطلة** ويبقى ميتاً —
   صار يختفي من شاشة الخدمات عند إيقاف `rates_screen`.
4. **زرا «👤 الملف» و«👁 معاينة الأسبوع» لم يكونا موصولين
   بميزاتيهما** (`quick_card`/`weekly_now` مفتاحان بلا أثر) —
   صارا يختفيان عند إيقاف الميزة.
5. **المبلغ المفضل يقبل قيماً خارج حدود الشحن** فيصير زر
   سريع يرفض دائماً — الآن يُرفض الحفظ خارج الحدود مع بقاء
   المستخدم بالسؤال.

### استُبعد بعد التحقق
- زوج `DepositFSM.photo` (صورة/نص تلميح) — ليس تكراراً.
- جميع بوابات `feat_on` معرفة بFEATURES، وكل الإعدادات
  المقروءة تُكتب عبر مسارات ديناميكية سليمة.

### فحوصات وتنبيهات
- الإصدار: `5.9.1 (AUDIT2)` — 354 معالجاً بلا تكرار فعلي.
- `tests/test_audit2.py` — 7 فحوصات (المجموع **152**:
  7+23+16+21+10+8+8+12+8+17+15+7).

---

## 29) منع النوم على Render [R6-PLUS11 — KEEP-ALIVE]

المشكلة: Render المجاني (Web Service) يُخمّد الخدمة بعد ~15
دقيقة بلا طلبات HTTP واردة، والبوت (polling) يصدر طلبات فقط
لا يستقبل — فيتوقف. **الحل من داخل البوت نفسه**:

### كيف يعمل
1. **خادم صحة مدمج**: عند الإقلاع على استضافة تضبط `PORT`
   (Render يضبطه تلقائياً) يفتح البوت خادماً صغيراً
   (بلا مكتبات إضافية) على `0.0.0.0:$PORT` يرد على أي طلب
   بـ200 + JSON فيه الحالة — يرضي فحص منفذ Render عند النشر.
2. **نبض ذاتي**: كل 60 ثانية (حسب طلب المالك — أقل بكثير من
   مهلة الخمول ~15 دقيقة) يرسل البوت طلب GET لعنوانه نفسه
   `/healthz` — وهذا طلب HTTP وارد يُعيد تصفير عداد الخمول
   عند Render **فلا ينام**.

### التفعيل (تلقائي)
- Render يوفر `RENDER_EXTERNAL_URL` تلقائياً → يكفي النشر
  بلا أي إعداد إضافي.
- استضافة أخرى؟ اضبط `KEEP_ALIVE_URL` على عنوان خدمتك العام.
- عناوين إضافية (مراقب خارجي مثلاً): `KEEP_ALIVE_URLS`
  مفصولة بفواصل.
- إيقاف الميزة كلياً: `KEEP_ALIVE=0`.
- **على جهازك المحلي لا شيء يتغير** — بلا PORT ولا عناوين
  لا يفتح البوت أي منفذ.

### إعداد Render خطوة بخطوة
1. New → Web Service → اربط مستودعك.
2. Runtime: Python 3 | Build: `pip install -r requirements.txt`
   | Start: `python bot.py`.
3. أضف متغيرات البيئة كاملة (كملف .env لديك): التوكن،
   ADMIN_USER_ID وغيرها.
4. انشر — ستجد بالسجل: `[keep-alive] خادم الصحة يعمل على
   المنفذ …` و`[keep-alive] نشط — نبضة كل 60 ثانية`.
5. للتحقق: افتح عنوان الخدمة بالمتصفح → `{"status": "ok", …}`.

### ⚠️ تحذير مهم: القرص المؤقت
الخطة المجانية على Render قرصها **مؤقت**: كل إعادة نشر
(وأحياناً إعادة تشغيل) تمحو `bot.db` وكل الملفات. الحماية:
- فعّل النسخة الاحتياطية اليومية (موجودة: §NEW 23) — تصلك
  قاعدة البيانات لتيلىجرام يومياً وتستطيع استرجاعها.
- أو رقِّ لخطة بقرص دائم (Persistent Disk) واربطه ب`/opt/render/project/src`.
- النبض الذاتي يمنع النوم — والنوم أحد أسباب فقد الملفات.

### فحوصات وتنبيهات
- الإصدار: `5.9.3 (KEEPALIVE-60s)` — نبضة كل 60 ثانية.
- `tests/test_keepalive.py` — 8 فحوصات (المجموع **160**):
  خادم الصحة 200/JSON فعلياً، النبض الذاتي، تسامح العناوين
  الميتة، بوابات التفعيل، وتحليل PORT بأمان.

### تحديث [AUDIT3]
بطلب المالك: الفاصل صار **60 ثانية** بدل 600 (`KEEPALIVE_INTERVAL = 60`).

## 30) ربط لوحة الوكيل [R6-PLUS12 — PANEL-LINK]

ربط مباشر بـAPI لوحة وكيل 55bets (نفس بيانات دخول المتصفح) عبر الوحدة
الجديدة `bets55_panel.py` — جلسة كوكيز مع إعادة دخول تلقائي عند انتهائها.

### متغيرات البيئة (إلزامية للتفعيل)
```
BETS55_PANEL_URL=https://agents.55bets.net   # أو العنوان الذي تملكه
BETS55_AGENT_USER=<اسم دخول اللوحة>
BETS55_AGENT_PASS=<كلمة المرور>
# اختيارية:
BETS55_CURRENCY=NSP
BETS55_COUNTRY=SY
```
بلا هذه المتغيرات يعمل البوت طبيعياً وتظهر اللوحة «غير مربوطة».

### الميزات الثلاث الجديدة (لوحة الميزات 49)
| ميزة | الوصف | افتراضي |
|---|---|---|
| `panel_link` | شاشة 🎰 لوحة الوكيل بأدوات الإدارة | 1 |
| `auto_create` | «إنشاء حساب» ينشئ اللاعب فعلياً باللوحة | 1 |
| `site_balance_live` | «معلوماتي» تعرض رصيد اللاعب الحي من اللوحة | 1 |

### ماذا تضيف
- **أدوات الإدارة → 🎰 لوحة الوكيل**: محفظة الوكيل والمتاح، قائمة
  اللاعبين، ➕ إنشاء لاعب (FSM)، 💸 إيداع للاعب و🏦 سحب من لاعب
  (اسم اللاعب ← المبلغ ← شاشة تأكيد ← تنفيذ + تدقيق + رصيد بعده).
- **auto_create**: عند تسجيل مستخدم جديد تُنشأ حسابته على الموقع فعلياً
  وتُحفظ مشفرة كالسابق؛ عند فشل اللوحة تظهر رسالة واضحة ولا يُحفظ شيء.
- **site_balance_live**: صفحة «معلوماتي» تجلب رصيد حساب المستخدم من
  اللوحة حياً (بند إضافي: 🎮 رصيدك بالموقع).

### ملاحظات تشغيلية
- إيداع/سحب اللاعبين عمليات رصيد حقيقية — خلف شاشة تأكيد ومسجلة في
  سجل التدقيق (`panel_deposit`/`panel_withdraw`/`panel_player_create`).
- تعليق اللوحة لا يوقف البوت: كل استدعاء مهلة 30ث ورسالة خطأ مقروءة.
- الاختبارات: `tests/test_panel_link.py` — 16 فحصاً على خادم لوحة
  مزيف (المجموع 176).

## 31) المطابقة الليلية + جسر شام كاش [R6-PLUS13 — RECON+BRIDGE]

### 🌙 المطابقة الليلية (ميزة `panel_recon` — افتراضي 1)
مهمة يومية في حلقة المهام الدورية: تجلب من اللوحة محفظة الوكيل
والمتاح وعدد اللاعبين ومجموع أرصدة اللاعبين، وترسل تقريراً للأدمن:
- قائمة اللاعبين بأرصدة غير صفرية (حتى 8 + العدد).
- عند فشل اللوحة: تقرير خطأ بدل التقرير (مرة واحدة يومياً).
- بلا بيانات بيئة: صامتة تماماً. قفل توقيت 24 ساعة
  (`last_panel_recon_at`) — قراءة فقط، لا أي تعديل على اللوحة.

### 🎰 حالة اللوحة في /ops
سطر جديد في تشخيص /ops: مربوطة؟ آخر تواصل، آخر خطأ، آخر محفظة
معروفة، عدد اللاعبين، وآخر مطابقة ليلية.
المصدر: `bets55_panel.panel_state()` (تتبع داخلي بلا ملفات).

### 🌉 جسر إشعارات شام كاش (ميزة `dep_bridge` — افتراضي 0)
نقطة API جديدة: `POST /api/webhooks/shamcash`
- نفس حماية بوابة الدفع: ترويسة `X-Signature` =
  HMAC-SHA256 للجسم الخام بمفتاح `PAYMENT_WEBHOOK_SECRET`.
- الحمولة: `{"telegram_id", "amount", "txid", "method?", "sender?", "raw?"}`.
- ما تفعله: تنشئ **طلب شحن معلقاً** ببطاقة كاملة (المستخدم/الطريقة/
  المبلغ/رقم العملية/المرسل/التفاصيل) تصل للفريق المالي بأزرار
  ✅/❌ الاعتماد بضغطة — **بلا أي إضافة رصيد تلقائية**.
- الحواجز: الميزة مطفأة افتراضياً، فحص `txid` مكرر بنفس الطريقة
  (منطق F8)، تحقق مستخدم/مبلغ.
- أكواد: 200 created/duplicate، 400 invalid، 404 user، 401 توقيع،
  503 الجسر مطفأ.

### اختبارات
`tests/test_phase4.py` — 14 فحصاً (المجموع **190**).

## 32) إعادة التسمية الشاملة إلى 55bets [BRAND]

تغيير العلامة في كل المشروع: `ichancy` / `iChancy` / `آيشانسي` /
`ايتشانسي` ← **55bets** (83 موضعاً في bot.py وحده):

- **الواجهة**: زر الدخول، قائمة الحساب، أقسام الإنشاء/الشحن/السحب،
  رأس الواجهة، رسائل الأقسام (عربي/إنجليزي/روسي).
- **الشروط والأحكام**: البند 5 (عربي + إنجليزي) — بقية البنود حرفية
  كما هي دون أي لمسة.
- **الشروحات**: «ما هو»، «إنشاء حساب»، «شحن»، «سحب» + رابط التعريف
  صار https://55bets.net.
- **تقنياً**: الوحدة `ichancy_panel.py` أصبحت `bets55_panel.py`،
  متغيرات البيئة أصبحت:
  ```
  BETS55_PANEL_URL   (افتراضي https://agents.55bets.net)
  BETS55_AGENT_USER
  BETS55_AGENT_PASS
  BETS55_CURRENCY    (NSP)
  BETS55_COUNTRY     (SY)
  BETS55_LOGIN_BASE  (تجاوز رابط بوابة الدخول)
  ```
- **المعرفات الداخلية**: callback data ومفاتيح النصوص وأسماء الدوال
  أعيدت تسميتها باتساق (menu_55bets، send_55bets_main، bets55_create…).

**رابط دخول اللاعبين**: `https://55bets.net` (بطلب المالك — 5.11.2)،
ودوال الدخول الآلي تبني عليه `?accounts=<المستخدم>&login=<كلمة_المرور>`.
قابل للتجاوز بالبيئة عبر `BETS55_LOGIN_BASE` إن تغير مسار البوابة.

## 33) حد رمز شام كاش 40 + دليل تفعيل الشحن التلقائي [TXID-40]

### الحد الجديد
رمز العملية (TXID) في الشحن: **الحد الأعلى 40 حرفاً أو رقماً** —
عند التجاوز تظهر رسالة واضحة (عربي/إنجليزي/روسي) تطلب إعادة الإرسال
بدلاً من القصّ الصامت السابق (كان 60 دون تنبيه). 40 بالضبط مقبول.

### دليل تفعيل شام كاش «تلقائياً» (الجسر — R6-PLUS13)
الواقع: لا يوجد API رسمي لشام كاش → «التلقائي» يعني: جسر يصنع
الطلب كاملاً + اعتماد الإدارة بضغطة ✅ (ولا رصيد بدون اعتماد).

1. **السر**: في `.env` تأكد من `PAYMENT_WEBHOOK_SECRET` (قوي وطويل) —
   نفس مفتاح بوابة الدفع الحالي.
2. **عنوان عام للجسر**: شغّل `api.py` (uvicorn) على خدمة ويب —
   على Render: Web Service بأمر
   `uvicorn api:app --host 0.0.0.0 --port $PORT`، والبوت Worker
   `python bot.py`. يصبح رابط الجسر:
   `https://<خدمتك>/api/webhooks/shamcash`.
3. **تفعيل الميزة**: لوحة البوت → الميزات → «🌉 جسر إشعارات شام كاش»
   (`dep_bridge`) — مطفأة افتراضياً.
4. **مصدر الإشعارات** (اختر واحداً):
   - تطبيق إعادة توجيه SMS على هاتف رقم شام كاش (SMS Forwarder)
     يرسل HTTP POST موقّعاً مباشرة، أو
   - تطبيق يمرر الرسائل لتليجرام + جسر صغير يقرأها ويرسل POST
     موقّعاً (يمكن بناؤه لاحقاً — البنية جاهزة).
5. **صيغة الطلب** (توقيع HMAC-SHA256 hex للجسم الخام):
   ```
   POST /api/webhooks/shamcash
   X-Signature: <hex>
   {"telegram_id": 123, "amount": 25.5, "txid": "AB123456",
    "sender": "اسم المرسل", "raw": "نص الرسالة"}
   ```
6. **اختبار**: أرسل طلباً تجريبياً (curl) وتأكد من وصول البطاقة
   للفريق المالي، ثم اعتمدها ✅ وتحقق من قيد الدفتر.
7. **الحواجز التلقائية**: تكرار txid بنفس الطريقة مرفوض (duplicate)،
   مستخدم غير موجود 404، مبلغ سيئ 400، الجسر مطفأ 503، توقيع خاطئ 401.

بديل بلا أي خادم: المسار الحالي — المستخدم يرسل الرمز بالبوت والبطاقة
تصل الفريق بالأزرار ✅/❌ (يعمل اليوم بدون خطوة واحدة من above).

## 34) ربط شام كاش بالـQR داخل تلجرام [R6-PLUS14 — SHAM-LINK]

ميزة `sham_link` (افتراضي 1) — زر **💵 شام كاش** بأدوات الإدارة:

### الربط (60 ثانية)
1. 💵 شام كاش ← 🔗 ربط حساب الآن → يرسل البوت **رمز QR داخل تلجرام**
   (بالصيغة الرسمية المطابقة لتوليد التطبيق — AES بالمفتاح الثابت +
   مفتاح جلسة داخلي + رمز FCM شكلي).
2. من التطبيق: حسابي ← الأجهزة المرتبطة ← إنشاء جلسة جديدة ← امسح
   الرمز (يظهر باسم Redmi Note 11 Pro) ← اعتمد.
3. مستطلع خلفي (كل ~2.2ث × 10 دقائق) يلتقط التوكنات، يفك تشفيرها
   بمفتاح الجلسة، يجلب الاسم، **يحفظها مشفرة (Fernet)** وحدّث الرسالة ✅.

### ما بعد الربط
- **💰 الأرصدة الحية**: قراءة مباشرة بكل العملات + المحجوز + المعلق.
- **🔁 إعادة ربط**: لاستبدال الحساب (بتأكيد) — نفس التدفق.
- **🗑 إلغاء الربط**: يحذف الجلسة محلياً + Session/logout (أفضل جهد).

### تقنياً
- وحدة `sham_link.py` مستقلة: تشفير هجين مطابق للموقع (AES-192-GCM
  بالمفتاح الثابت + لف RSA-PKCS1v15)، Session/check و Account/*،
  وفك encData بمفتاح الجلسة.
- الجلسة المخزنة: admin_settings/sham_session (Fernet) — لا تُطبع أبداً.
- انتهاء الجلسة (من التطبيق أو زمنياً) = إعادة ربط بدقيقة.
- متطلب جديد: `qrcode[pil]>=7.4` (أُضيف للمتطلبات).

### الأمان
- الربط بيد الأدمن وحده وباعتماد من صاحب الحساب على هاتفه.
- مفتاح الجلسة يبقى بالرمز فقط — من يمسك الرمز يستطيع الاعتماد فقط
  (الاعتماد بيد صاحب الحساب). إلغاء فوري من التطبيق بأي لحظة.

### تحديث [SHAM-LINK2 — 5.12.1]
بعد محاولة ربط فعلية أعطى «تم» من التطبيق دون تسجيل بالخادم، أضيف
تشخيص حي كامل لعملية الربط:
- رسالة الانتظار تتحدث كل ~30ث: عدد المحاولات + **آخر رد حرفي من
  خادم شام كاش** (مثل «الجلسة غير موجودة. (1231)»).
- تحذير صريح بعد 10 محاولات بلا اتصال (مشكلة إنترنت/VPN بجهاز البوت).
- زر **🔄 فحص الآن** (استطلاع فوري عند الطلب بعرض الرد) وزر **❌ إلغاء**.
- رسالة انتهاء المهلة تعرض السبب + الأسباب الشائعة (أشهرها: مسح رمز
  قديم من محادثة سابقة، أو اعتماد بعد انتهاء صلاحية الرمز).
- توثيب داخلي: check_session_ex يعيد وصف الرد؛ الاختبارات 16.

### تحديث [DIAG-PLUS — 5.12.2]
بعد أول تواصل حي مع لوحة الوكيل (فشل «انتهت الجلسة») وفحص شبكة شام كاش:
- **لوحة الوكيل**: المسارات والبيانات تأكدت حياً (signIn ← معلومات
  الوكيل ← اللاعبون). إصلاحات الوحدة:
  - رفض الدخول الحقيقي (status:true + result:false) كان يُعتبر نجاحاً —
    صُحح: رسالة «بيانات الدخول غير صحيحة (…)» مع نص الخادم.
  - 401/403 مع رد HTML = حجب جدار حماية/Cloudflare — رسالة واضحة
    «اللوحة تحجب الوصول من هذه الشبكة» بدل «انتهت الجلسة» المضللة.
  - إعادة الدخول التلقائي صارت بنوع استثناء متميز PanelAuthError.
  - هيدرز متصفح (UA/Origin/Referer/XRW) لكل الطلبات.
- **شام كاش**: فحص وصول أولي (8 ثوانٍ) قبل عرض QR — بلا وصول لا QR
  بل رسالة أسباب (إنترنت/جدار حماية/جرب نقطة هاتف) + زر إعادة.
  «فحص الآن» بمهلة 12 ثوانٍ ونتيجته برسالة جديدة (الجواب الثاني على
  نفس الضغطة يرفضه تلجرام «query is too old»).

### تحديث [MENU-ORG — 5.12.3]
إعادة تنظيم قوائم الإدارة بناءً على طلب الأدمن:
- **💵 ربط شام كاش (QR)** انتقل من «🧰 الأدوات» إلى داخل
  «⚙️ إدارة شام كاش» (أول زر فيها) — ورجوع شاشات شام أصبح لتلك الشاشة.
- **🎰 لوحة الوكيل** انتقلت من «🧰 الأدوات» إلى القائمة الرئيسية
  **بجانب «👑 لوحة تحكم الأدمن»** في صف واحد.
- **📱 إدارة سيرياتل كاش** — قسم جديد بجانب «⚙️ إدارة شام كاش» يجمع:
  تغيير رقم الإيداع + رمز الإيداع (جديد، يُخزن ويُعرض حسب النمط) +
  ما يظهر للمستخدم (رقم/رمز) + قبول/إيقاف الشحن + حركة اليوم.
- فك التكرار: أزرار المحافظ (سيرياتل/شام أوتو، الأرقام، الرموز)
  أُزيلت من «🤖 إدارة البوت» وبقيت بشاشاتها المتخصصة فقط؛
  «أكواد سيرياتيل اليدوية» القديمة صارت تفتح شاشة الرقم الجديدة.
- إصلاح أزرار رجوع معطلة (callback بلا معالج) في شاشات
  لوحة الوكيل وشام كاش — تدقيق كامل: 253 زراً وكلها لها معالج.

### تحديث [AGENT-FULL — 5.13]
استكشاف حي كامل لموقع لوحة الوكيل (من كود الواجهة + اختبار نقاط
النهاية ببيانات الوكيل) ثم إضافة كل ما يملكه الحساب صلاحية له:
- **جديد — 💼 المحافظ والحركات** (ميزة panel_wallets، افتراضي 1):
  شاشة محافظ الوكيل بكل العملات (متاح للسحب/رصيد/مكافآت/مجمّد) +
  العملات المتاحة + آخر حركة على المحفظة، وشاشة سجل الحركات.
  المسارات الحية المؤكدة: Agent/getAgentAllWallets وAgent/getAgentWallet
  وAgent/getAgentTransactionList وAgent/getAgentAvailableCurrencies.
- ترويسة لوحة الوكيل تعرض الآن «متاح للسحب» من المحافظ الحقيقية
  (NSP 20,892.50 وقت الفحص) — أدق من currentWallet القديم.
- الجرد الحي للصلاحيات بهذا الحساب: اللاعبون كاملون (قائمة/إنشاء/
  إيداع/سحب/رصيد) ✓ — التقارير والحدود والمكافآت وسجل تحويلات
  اللاعبين وروابط التسويق: **باقة الحساب لا تمنحها** (الرد من الخادم:
  «you don't have permission») والموقع نفسه لا يعرضها.
- FEATURES=53 (أضيف panel_wallets)؛ فحوص اللوحة 21.

### تحديث [CODE-40 — 5.13.1]
رموز الحساب التي يدخلها الأدمن (رمز حساب شام كاش + رمز إيداع
سيرياتل كاش) أصبحت تقبل حتى **40 خانة** بدل 30 — لمواءمة طول رموز
شام كاش الفعلية. أرقام المحافظ (الهواتف/المعرفات) تبقى بحد 30.

### تحديث [PLAYER-CREATE — 5.13.2]
بعد رصد نداء الموقع الحقيقي من أدوات المطور (بتعاون الأدمن)،
اكتُشفت الصيغة الفعلية لإنشاء اللاعب ونُقلت حرفياً للبوت:
- الجسم مغلّف: {"player": {login, password, email, parentId}}
- login بدل userName — والبريد الإلكتروني **إلزامي وفريد**
  (يولّده البوت عشوائياً @gmail.com — المكرر يُرفض Duplicate email)
- parentId = رقم الوكيل فقط (يُجلب تلقائياً من معلومات الحساب،
  ويمكن تجاوزه بـ BETS55_PARENT_ID)
- أُثبت حياً: إنشاء لاعبين تجريبيين تحت وكالة Saleh123 بنجاح
  (bottest9001 وbottest9002 — يمكن حذفهما من الموقع)
- نفس الوحدة تخدم إنشاء الحساب الآلي عند الشحن (auto_create).

### تحديث [AUTO-DEP — 5.14]
الشحن الآلي لشام كاش (P2) — مطابقة الوارد بالطلبات المعلقة:
- مراقبة `Transaction/history-logs` كل 60 ثانية (الوارد tranKind=1)،
  ومطابقة رقم العملية (strTranId) + المبلغ مع الطلب المعلق.
- عند التطابق: اعتماد ذرّي عبر نفس مسار الأدمن (عمولة/مكافأة/نقاط)،
  تعليم الطلب بـ auto=1، إشعار المستخدم والأدمن، وتسجيل بالتدقيق.
- **الحمايات**: العملية تُستهلك مرة واحدة (لا تكرار أبداً)؛ أول تشغيل
  بعد التفعيل يرصد فقط بلا اعتماد (التاريخ القديم يُتجاهل)؛ عتبات
  (حد أدنى/أقصى/سقف يومي) خارجها يبقى يدوياً؛ الوارد بلا طلب معلق
  يُبلَغ مرة واحدة ولا يُعتمد.
- الميزة **مطفأة افتراضياً** (sham_auto_dep=0): التفعيل من
  ⚙️ إدارة شام كاش ← 🤖 الشحن الآلي بعد ضبط العتبات وربط الجلسة.
- الشاشة تعرض الحالة/آخر فحص/اعتمادات اليوم/العتبات وتبديلها.
- FEATURES=54؛ فحوص شام 26 (5 جديدة للتدفق الآلي).

### تحديث [WD-40 — 5.14.1]
وجهة سحب شام كاش/سيرياتل كاش تقبل حتى **40 خانة**:
- المعرفات الحرفية/الرمزية (أحرف وأرقام ورموز) مقبولة حتى 40 —
  كانت تُرفض قاطعاً («bad») لأي حرف ضمن العنوان.
- الأرقام غير القياسية (طويلة حتى 40 أو قصيرة) تنتقل للتأكيد
  (doubt) بدل الرفض القاطع؛ أرقام السورية القياسية تبقى
  تتطابق مباشرة (+963…) والرفض لفوق 40 أو أقل من 5 خانات.
- مفعّل بميزة wd_dest_check — إبقاء سلوك التطبيع الكامل.

### تحديث [PANEL-STR — 5.14.3]
إصلاح عطل ميداني: انهيار لوحة الوكيل بـ «خطأ غير متوقع:
AttributeError» عند انتهاء جلسة PHP داخلية باللوحة (الخادم يرد
200 بنص بدل البيانات). الإصلاح:
- أي «نتيجة نصية» تعامل كجلسة منتهية → تسجيل دخول تلقائي جديد
  وإعادة الطلب مرة واحدة (داخل القفل) ثم خطأ واضح إن استمر.
- agent_info بحزام أمان ضد الأنماط غير المتوقعة.
- بلا أي تغيير بالواجهات — والفحص الجديد يحاكي العطل الحقيقي
  (تدهور ثم تعافٍ فوري).

### تحديث [PROXY-OUT — 5.15]
تجربة Frankfurt فشلت كلاهما (الحجب شامل لعناوين Render) — أُضيف
بروكسي صادر اختياري كامل:
- `OUTBOUND_PROXY` (عام) أو `BETS55_PROXY` / `SHAM_PROXY` لكل خدمة.
- الصيغ: http(s)://user:pass@host:port — وsocks5:// عبر aiohttp-socks
  (أضيفت للمتطلبات).
- على Render: ضع القيمة في Environment Variables وأعد النشر.
- التحقق بعد التفعيل: /ops ← «اللوحة: ✅» وربط QR شام جديد.
- تمرير عبر بروكسي HTTP حقيقي مُختبر بالفحوص (دخول+جلسة عبر
  بروكسي محلي)؛ بروكسي ميت يعطي خطأ واضحاً بلا انهيار.
- ملاحظة صريحة: بروكسي Datacenter قد تحجبه الحواجز أيضاً — إن حدث
  بدّل رابط البروكسي فقط (نفس المتغير) لبروكسي Residential بلا
  أي تغيير بالكود.

### تحديث [AUTO-WD — 5.16]
السحب الآلي لشام كاش: مراقبة طلبات السحب المعلقة (عمرها ≥ 60ث) ←
اعتماد الطلب (خصم رصيد البوت + أمر دفع معلق) ← تحويل شام كاش للوجهة ←
تحقق بمرجع من رد الإرسال أو السجل الصادر (tranKind=2) ← تعليم أمر
الدفع «مدفوع» بمرجع sham:…. الحواجز الإلزامية:
- تفعيل للأدمن الرئيسي فقط وبـPIN (يُعيَّن من الشاشة، hash مخزّن).
- عتبات: shamwd_min/max + سقف يومي للمنظومة + سقف لكل مستخدم.
- كل طلب يُلمس مرة واحدة (وسم wdauto=…) — لا إعادة إرسال أبداً.
- أي فشل إرسال/نتيجة غامضة = تجميد فوري للميزة + إشعار فوري؛
  أمر الدفع يبقى معلقاً (لا يُعلَّم مدفوعاً أبداً بلا مرجع).
- الشاشة: إدارة شام كاش ← «🤖 السحب الآلي». مطفأ افتراضياً.
- تنبيه: نظام قائمة الثقة القديم (auto_wd_enabled) يعلّم أوامر
  الدفع «مدفوعة» بلا إرسال فعلي — عطّله قبل استخدام الجديد.
- ملاحظة صريحة: صيغة Exchange/createTransactionToSomeone مستنتجة
  من التحليل الساكن — أول محاولة حية ستثبت الحقول (تصحيح محتمل
  في sham_link.send_transfer حصراً) وصلاحية جلسة المحرر للإرسال
  لم تُختبر حياً بعد؛ التجميد التلقائي يحميك في الحالتين.

### ترقيع [AUDIT — 5.16.1]
تدقيق هجومي كامل للمحركين (12 فحصاً جديداً) كشف وأصلح ثغرة
حرجة بالشحن الآلي: المحرك كان يطابق رقم العملية فقط — مستخدم
يُنشئ طلب شحن بمبلغ كبير ويحوّل مبلغاً أقل بنفس رقم العملية
كان يتحقق طلبه كاملاً. الآن: عدم تطابق مبلغ التحويل مع مبلغ
الطلب = لا اعتماد + تنبيه واحد للأدمن والطلب يبقى للمعالجة
اليدوية. فحوص التدقيق تغطي أيضاً: تكرار رقم العملية، تعايش
المحركين، الطلبات الطازجة (<60ث)، حدود السقف، الردود الشاذة،
الطلبات المحتجزة (held=1)، منع إعادة معالجة ما اعتمده الأدمن
يدوياً، وتفرد تنبيه السقف اليومي.

### ترقيع [WIRE — 5.16.2]
إصلاح كابل تشغيل السحب الآلي: نسخة 5.16.1 وُزّعت وزر «🤖 السحب
الآلي» وسجل FEATURES وسطر تشغيل الحلقة الدورية كانت مفقودة
(تعديلات متوازية على bot.py أسقطت بعضها) — المحرك كان موجوداً
ومختبراً لكنه غير قابل للوصول من الواجهة. أُضيف فحص W8
ال/regression/: الافتراضي «0» + انطلاق الحلقة مع main + وجود
الزرين بلوحة إدارة شام كاش. ملاحظة مهمة: إن رقّيت من 5.16.1
فالميزة غير مسجلة عندك بستّtings — بعد التحديث تأكد أنها 🔴
متوقف من الشاشة قبل أي شيء (الافتراضي الجديد إيقاف).

### ترقيع [CHPOST — 5.16.3]
إصلاح ميزة «نشر القنوات والمجموعات»: الرسالة «البوت لم يتمكن من
الوصول للقناة» كانت تظهر لأن التطبيع كان يقبل @اسم/رقم فقط — أي
رابط t.me يُمرر كما هو فيفشل، وروابط الدعوة الخاصة (t.me/+…)
لا تكفي للعنونة أصلاً. الجديد:
- تطبيع شامل: أي صيغة رابط → @اسم؛ t.me/c/1234567 → -1001234567.
- رابط دعوة خاص → شرح الحل (رقم -100 عبر إعادة توجيه منشور
  إلى @getidsbot) بلا حفظ.
- تحقق حي قبل الحفظ: get_chat + فحص العضوية/الصلاحية → رسائل
  دقيقة (مشرف بلا صلاحية نشر / عضو مجموعة / غير موجود).
- الفشل لا يطمس القناة السابقة — بقاؤها مع التشخيص.
- مزامنة الجدولات: تعيين قناة جديدة يحدّث صفوف channel_posts
  القديمة (كانت تبقى على القناة القديمة للأبد).
- «اختبار النشر» يعطي تلميح السبب في رسالة الفشل.

### توثيق واختبار [API-SYS — 5.16.4]
نظام الـAPI التلقائي (زر إدارة البوت): زر حالة يعرض إحصاءات
الاستقبال الآلي (أحداث 24س + إجمالي الشحن المُحتسب). مكوّناته:
- V1 — بوابة الدفع (POST /api/webhooks/payment): شحن تلقائي
  مباشر. أمان: HMAC-SHA256 (X-Signature) بمفتاح
  PAYMENT_WEBHOOK_SECRET — بلا مفتاح: معطل كلياً.
  Idempotency: نفس event_id لا يُحتسب مرتين؛ نفس المعرف بمحتوى
  مختلف = 409؛ الأحداث غير الناجحة تُؤكد بلا أثر.
- V2 — جسر شام كاش (POST /api/webhooks/shamcash): يحتاج ميزة
  dep_bridge (مطفأة افتراضياً)؛ ينشئ طلب شحن معلقاً بضغطة
  اعتماد — لا يضيف رصيداً تلقائياً بالتصميم؛ dedup برقم
  العملية؛ المحظور مرفوض.
- إضافة: payouts API (سحب من موقع خارجي) بمفتاح X-API-Key
  وIdempotency-Key، وفشل التحويل لاحقاً يسترد المبلغ تلقائياً.
- نقطة الصحة: GET /api/health.
اختبار: 16 فحصاً (التوقيعات، الصلاحية، التكرار، التعارض،
المبالغ المرفوضة، المحظور، إحصاءات الزر، المعرفات).
ملاحظة تشغيلية: V1 يحتاج بوابة دفع خارجية وV2 يحتاج جسر
إشعارات (SMS→تلجرام) — كلاهما اختياري والبوت يعمل كاملاً بدونهما.

### تحديث [RAND-DEV — 5.17]
عند كل ربط شام كاش جديد يُولَّد اسم جهاز عشوائي من مجموعة
أسماء أندرويد واقعية شائعة (20 اسماً: Redmi/Galaxy/Huawei/
Oppo/Infinix…) — بدل الاسم الثابت «Redmi Note 11 Pro» لكل
الربطات. الغرض: منع بصمة «نفس اسم الجهاز» بين نسخ متعددة
تعمل من جهاز واحد. تفاصيل:
- الاسم يُعرض للمالك قبل المسح («سيظهر باسم: …») ويثبت لصالح
  الجلسة حتى انتهائها (يظهر في شاشات الحالة كما هو).
- الاسم الصريح يبقى مدعوماً (توافق خلفي) والمسبار بلا تغيير.
- الفحوص: +3 (عشوائية/توافق/كابل) = 288 إجمالاً.

### ترقيع [DEVHDR — 5.17.1]
مكمل RAND-DEV: الهندسة العكسية أثبتت أن التطبيق الأصلي يرسل
اسم جهازه بترويسة deviceName مع كل طلب — صار بوتنا يفعل الشيء
نفسه: اسم الجلسة العشوائي يسافر بترويسة كل نداء (authed_call
من الجلسة، Session/check من الرابط) — فيطابق بصمة التطبيق
الأصلي على مستوى كل طلب لا الجلسة فقط. الجلسات القديمة بلا
حقل device ترسل بلا ترويسة (توافق خلفي كامل — كما كانت).
فحوص: +2 (ربط الترويسة + التقاطها فعلياً بخادم محلي) = 290.

### تحديث [EVDEP — 5.18] بقرار المالك: بلا متغير وضع
إعادة تصميم الشحن الآلي من «دورة زمنية كل 60ث» إلى نمط حدثي
يقلص نبضات شام كاش من عنوانك ~97%:
- حُذفت حلقة الاستطلاع الدوري كلياً (لا رجوع لها).
- عند تسجيل أي طلب شحن شام كاش تُفتح نافذة تحقق من 10 نبضات
  متراكمة: فور الطلب ثم 1، 2، 4، 6، 9، 12، 15، 20، 25 دقيقة —
  أول مطابقة (رقم العملية + المبلغ) تعتمد وتوقف النافذة فوراً.
- لا مطابقة حتى نهاية النافذة → الطلب يبقى معلقاً للفحص اليدوي
  (زر فحص موجود) أو ينقذه المسح اليومي.
- مسح أمان واحد يومياً بإزاحة عشوائية (أول مسح 6-12س بعد الإقلاع
  ثم كل 22-26س): ينقذ الطلبات الفائتة، يرصد الوارد الشارد (إشعار
  مرة واحدة)، وأول مسح بعد التفعيل رصد فقط.
- إشعارات عدم التطابق/الخروج من العتبات/السقف: مرة واحدة لكل
  طلب (حرس seen) — لا إزعاج متكرر.
- كل الحمايات كما هي: المطابقة المزدوجة، الاستهلاك مرة واحدة،
  العتبات والسقوف، أول-تشغيل-يرصد، التدقيق.
- السحب الآلي بلا تغيير (حلقته تفحص قاعدة البيانات المحلية ولا
  تلمس شام إلا عند تنفيذ فعلي).
- إعادة تشغيل البوت: النوافذ الفائتة ينقذها المسح اليومي.
- الوارد بلا طلب مسجل لا يُكتشف فورياً (ثمن إسكات المراقبة) —
  يظهر في المسح اليومي.

### تحديث [WGT — 5.18.1] حارس محفظة شام كاش
زر جديد بلوحة إدارة شام كاش: «🛡 مقارنة رصيد المحفظة مع البوت»:
- تشغيل/إيقاف (مطفأ افتراضياً) + تحكم بعدد فحوص اليوم (1-6،
  يدور بزر واحد) — والاستهلاك اليومي معروض بالشاشة.
- بلا أي نبضات إضافية: الفحص يُنفَّض ضمن نوافذ طلبات الشحن
  الموجودة + نبضة المسح اليومي (مطابق لقرار EVDEP).
- المقارنة: رصيد المحفظة الفعلي مقابل المتوقع (آخر رصيد معروف
  − مدفوعات شام الصادرة + شحنات auto المعتمدة بعده) — أي انحراف
  = إنذار فوري (مرة كل ساعتين كحد أقصى لكل نوع).
- إنذار موت الجلسة: عند فشل قراءة الحارس يُخبر الأدمن فوراً أن
  الجلسة لا تستجيب وأن الأتمتة متوقعة حتى إعادة الربط.
- الشاشة تعرض: الحالة، الربط، استهلاك اليوم، آخر قراءة والمتوقع.

## 35) تحديث [BREATH+PHIDE — 5.18.2] تنفّس السحب + إخفاء لوحة الوكيل
**أ) تنفّس السحب الصادر [WD-BREATH]:**
- داخل نبضة السحب الآلي: إذا نُفِّذ تحويل صادر، فإن أي تحويل
  تالٍ بنفس النبضة ينتظر فاصلاً عشوائياً 20-60 ثانية قبل الإرسال
  (إيقاع بشري، `secrets.randbelow`).
- الأدمن يتلقى إشعاراً «⏳ تنفّس …ث قبل السحب التالي» قبل كل فاصل.
- لا فاصل قبل التحويل الأول — النبضة أحادية تبقى كما هي.

**ب) إخفاء/إظهار لوحة الوكيل [PHIDE]:**
- مفتاح جديد `feat_panel_hidden` (مطفأ افتراضياً = اللوحة ظاهرة).
- من الإعدادات ← الأدوات: زر حالته يتبع الوضع:
  «🙈 إخفاء لوحة الوكيل» / «🎰 إظهار لوحة الوكيل».
- عند الإخفاء: زر «🎰 لوحة الوكيل» يختفي من القائمة الرئيسية
  لأي مدخل (حتى الأدمن)، وأي محاولة فتح مباشرة تُرفض برسالة
  تنبيه — بدون إعادة تشغيل.
- عند الإظهار: يعود الزر وكل الشاشات تعمل فوراً بشكل طبيعي.
- أزرار الإخفاء/الإظهار للمدخل الرئيسي (ADMIN_USER_ID) فقط،
  مع شاشة تأكيد بعد كل عملية.
- لوحة الأدمن المستقلة (admin_home) غير متأثرة — الخصوصية
  تستهدف لوحة الوكيل (55bets) فقط.

## 36) ترقيع [LINK-REVERT — 5.18.3] إصلاح فشل ربط شام كاش (401)
**الظاهرة الحية:** الربط على 5.18.2 يفشل مرتين متتاليتين برده
«وصول غير مصرح به. يرجى تسجيل الدخول مرة أخرى. (401)» حتى بربط
جديد كلي بلا جلسات سابقة، بينما نجح ربط فوري على 5.16.3 بنفس
الحساب والهاتف واللحظة.

**التشخيص:** diff حرفي لملف sham_link.py بين النسختين — الفروق
اثنان فقط: عشوائية اسم الجهاز (5.17) وترويسة deviceName مع كل
طلب (5.17.1). كل شيء آخر (تشفير/أجسام/نقاط/ترويسات) متطابق.

**العلاج (قرار المالك: إرجاع الترويسة فقط):**
- أُزيلت ترويسة deviceName من كل الطلبات (الاستطلاع والمحمية)
  — عادت بصمة الطلب مطابقة لـ5.16.3 المُجرَّبة حياً مرتين.
- بقيت عشوائية اسم الجهاز لكل ربط (RAND-DEV 5.17) كما هي.
- الفحوص 30 و31 عُكست لتثبت السلوك المرجَع (لا ترويسة إطلاقاً)
  — المجموعة 307 فحوصاً.
- الخلاصة العملية: عند أي فشل ربط مستقبلي: ابدأ ربطاً جديداً
  وامسح رمز الرسالة الجديدة خلال دقيقتين؛ الرموز تعمل مرة واحدة.

## 37) تحديث [UI-TABS — 5.18.4] ترتيب الواجهات وتبويباتها
**فحص شامل سابق للتنفيذ:** 57 ميزة + 523 زراً — النتيجة:
ميزة واحدة ميتة («ربط لوحة الوكيل»: بلا أي قراءة بالكود،
وظيفتها استوعبتها PHIDE وربط env) وصفر أزرار ميتة.

**أ) مركز تفعيل الميزات:**
- زرين بالسطر بدل قائمة عمودية واحدة (شاشتا الفتح وإعادة الرسم).
- حُذفت الميزة الميتة → المركز 56 ميزة كلها موصولة فعلاً.
- اختصار 38 تسمية طويلة (المفاتيح والسلوك دون تغيير).

**ب) تبويبات الإعدادات (بدل 25 زراً مسطّحاً):**
- الجذر: 🎛 تفعيل الميزات + 4 تبويبات + رجوع.
- 💰 المالية والحدود: عمولة، حدود وسقوف، ميزانية سحب، مكافأة
  إيداع، محاكي الشحن، اعتماد تلقائي، إعدادات القيم.
- 👥 المستخدمون والولاء: الولاء، الحضور، مكافأة الإحالة، موافقة
  مزدوجة، كشف الشواذ، إخفاء الأرصدة (بحالته الحية).
- 📢 المحتوى والنصوص: النصوص، روابط القائمة، ترحيب بصورة، ساعات
  الدعم.
- 🧯 التشغيل والصيانة: صيانة مجدولة، نطاقها، إيقاف الخدمات، ZIP
  يومي، غرفة المراقبة، رمز التأكيد.

**ج) تبويبات الأدوات (بدل 13 زراً مسطّحاً):**
- الجذر: 3 تبويبات + مسابقة الإحالات + زر إظهار/إخفاء لوحة
  الوكيل (يتبع الحالة — مواصفة PHIDE) + رجوع.
- 📣 البث والإعلانات: بث، إعلانات، رسالة اختبار.
- 🗄 البيانات والنسخ: ZIP، نسخة الآن، تنظيف، تدقيق، ويب هوك.
- 📊 التقارير والفحص: رسم بياني، تقرير محاسبي.

**ملاحظات تقنية:** كل التبويبات تعيد استخدام نفس معالجات
الشاشات القائمة حرفياً (لا تغيير وظيفي)، فحوص wiring تُحدّث
لتتبع البنية الجديدة، المجموعة 307 فحوصاً.

## 38) تحديث [DEDUP — 5.18.5] دمج الأزرار المكررة وإعادة التجميع
**فحص شامل لكل شاشات البوت** (الأدمن بثلاثة مستويات + المستخدم +
شام + سيرياتل + لوحة الوكيل) بتتبع كل زر إلى معالجه.

**أ) ازدواج حقيقي حُذف (5 أزرار):**
- «📈 إحصائيات الكاشيرة» (الصفحة الإدارية): كانت تستدعي «📊 KPI
  حي» حرفياً — البديل يبقى باللوحة الكاملة.
- «📢 إرسال إذاعة» (إدارة البوت) = «📣 بث رسالة» (الأدوات) بنفس
  المعالج — البقاء لأدوات البث.
- «⚙️ إعدادات نظام الإحالة» (إدارة البوت) = «🤝 مكافأة الإحالة»
  (الإعدادات←المستخدمون) بنفس الشاشة.
- «🎁 الرجوع للغائبين» (إدارة البوت): كان يبدّل feat_winback
  نفسه الذي يبدّله زر مركز الميزات — المرجع الموحد: المركز.
- شاشة «الإحصائيات العامة» الزائدة: حُدّثت لتفتح شاشة الإحصائيات
  الموحدة الأغنى (وأصبح معالجها القديم aliasاً لأزرار الرسائل
  القديمة — نمط referral_settings المجرَّب).

**ب) توضيح ازدواج شبيه (مهمتان مختلفتان — بلا دمج):**
- «📦 جدولة ZIP اليومي» (الإعدادات←التشغيل): تفعيل/إيقاف
  التصدير الآلي.
- «📦 تصدير ZIP الآن» (الأدوات←البيانات): تصدير فوري.

**ج) إعادة التجميع:**
- الصفحة الإدارية: صفوف الأقسام (مستخدمون|بوت، شام|سيرياتل،
  شحن وعروض|أرصدة المحافظ) ثم البيانات (إحصائيات|تقرير مخصص)
  ثم اللوحة الكاملة ثم رجوع.
- إدارة البوت: التشغيل (إيقاف/تشغيل|نظام API) ثم المحتوى
  (نشر القنوات|تذكير العالقة) ثم التقييمات ثم رجوع — 6 أزرار
  بدل 9.

**ملاحظات:** قائمة المستخدم وشام وسيرياتل واللوحة فُحصت وخرجت
بلا أي تكرار. المجموعة 307 فحوصاً + دخان حي يثبت الحذف والتوحيد
على المعالجات الفعلية.

## 39) توثيق [RESTORE — 2026-09-12] العودة إلى 5.18.5 بقرار المالك
**القرار**: إلغاء سطر بيئة التجربة كاملاً (5.18.6 SANDBOX ← 5.18.12 SBXBTN) والعمل من 5.18.5. التنفيذ: قصّ جراحي من الكود الحالي (لا توجد حزمة 5.18.5 أصلية) — زر 🧪 ومعالجات sbx_* و_run_tour_in_chat وself_module من bot.py، وحذف tests/test_sandbox.py ومجلد sandbox/؛ الجولة التعريفية الحقيقية (tour slides) لم تُمس. الحالة الكاملة قبل التراجع مؤرشفة في archive_5.18.12/.
**التحقق**: مطابقة سطر-بسطر مع ملف 5.18.4 (UI-TABS) الحقيقي من جهاز المالك — 15 موضع فرق/26,399 سطراً (99.92%)، كلها بنود §38 (DEDUP) حرفياً وصفر إشارات بيئة. **الاستثناء المعتمد («أ»)**: بقي [FIX 5.18.6] صفر الصفوف (صف بحجم 0 بتقسيم قائمة الموثوقين ثلاثياً) — إصلاح عيب مستقل. الهوية النهائية: 5.18.5 (DEDUP+). المجموعة 307 فحصاً.

## 40) تحديث [SETTOOLS — 5.18.13] دمج الأدوات في الإعدادات + الإعدادات بلوحة الأدمن
**بطلب المالك:**
1) **الدمج**: محتويات «🧰 الأدوات» كلها انتقلت إلى جذر «⚙️ الإعدادات» الذي صار «الإعدادات والأدوات» (11 زراً: ميزات + 4 تبويبات إعدادات + 3 تبويبات أدوات + مسابقة الإحالات + PHIDE + رجوع). شاشة جذر الأدوات اعتزلت: `admin_tools_kb` حُذفت، ومعالج `admin_tools_menu` صار aliasاً يفتح الجذر الموحد — فكل أزرار «رجوع للأدوات» القديمة (17 موضعاً) تعمل وتفتح الإعدادات، وتبويبات الأدوات (`tools_tab:*`) كما هي بمعالجاتها الحرفية وعناوينها صارت «الإعدادات — …».
2) **النقل**: زر «⚙️ الإعدادات والأدوات» أُضيف إلى `get_admin_home_kb` (لوحة تحكم الأدمن) بجوار «اللوحة الكاملة»، وأُزيل زرا «⚙️ الإعدادات» و«🧰 الأدوات» من الصفحة الإدارية (نسختيها `admin_panel_kb` و`admin_panel_kb_live`).
**النصوص**: مسارات الإرشاد حُدّثت («الإعدادات ← الأدوات» أصبحت «الإعدادات») برسائل الرفض وإعادة الإظهار وتسميات الرجوع. **الفحص**: t4 حُدّث ليؤكد الدمج على الجذر الموحد وتتبع الحالة — المجموعة 307 فحصاً.

## 41) تحديث [PANELHUB — 5.18.14] دمج اللوحة الكاملة في إدارة البوت + الميزات بلوحة الأدمن
**بطلب المالك:**
1) **الدمج**: محتويات «🎛 اللوحة الكاملة» (admin_panel) الثمانية انتقلت إلى «🤖 إدارة البوت» عبر `admin_manage_bot_kb()` الجديدة (معالج + رندر يستخدمها): الطابور الموحد، المستخدمون، المالية، الهدايا، المشرفون، الإحصائيات، KPI حي، التقارير — مع الحفاظ على عدادات live_counters الحية. شاشتا اللوحة (`admin_panel_kb`/`admin_panel_kb_live`) حُذفتن؛ معالج admin_panel صار aliasاً لإدارة البوت فتخدم كل أزرار الرجوع القديمة إليه (مستخدمون/مالية/هدايا/مشرفون/تقارير/KPI/طابور — أبناؤها الفعليون الجدد). أمر /admin يفتح لوحة تحكم الأدمن.
2) **النقل**: زر «🎛 تفعيل الميزات» أُزيل من جذر الإعدادات وأُضيف إلى لوحة تحكم الأدمن (بجوار الإعدادات والأدوات)؛ رجوع كشف الميزات (الرسمَين) صار للوحة الأدمن.
**التصحيحات المرافقة**: رجوع جذر الإعدادات → لوحة الأدمن (والدها الحقيقي)؛ رجوعا شاشتي النصوص والحدود → الإعدادات (والدهما). فحوص t4/r6plus9-t11/r6rev-t10 حُدّثت للبنية الجديدة — المجموعة 307 فحصاً.

## 42) ترقيع [USERDUP — 5.18.15] حذف زر «المستخدمون» المكرر من إدارة البوت
**الطلب**: فحص «👥 المستخدمون» (إدارة البوت) مقابل «👥 إدارة المستخدمين» (لوحة الأدمن). **النتيجة**: مكرر تام — `admin_manage_users` alias نقي يستدعي `admin_users`، وكلاهما يفتح نفس شاشة «👥 إدارة المستخدمين» بنفس `admin_users_kb` ونفس الصلاحية (بقايا قائمتا R6 المتوازيتان). **التنفيذ** (بقرار المالك: الحذف): زر «👥 المستخدمون» أُزيل من `admin_manage_bot_kb` (14 زراً) وتوزيع الصفوف ضُبط؛ أزرار الرجوع الداخلية لقائمة المستخدمين لم تُمس. الفحص r6rev-t10 حُدّث ليؤكد الغياب + وجود الباب الوحيد بلوحة الأدمن — المجموعة 307 فحصاً.

## 43) ترقيع [WDFIX — 5.18.16] عمولة السحب لم تكن تُحصَّل — إصلاح كامل لمسار الرصيد
**الملاحظة الحية من المالك**: عند طلب سحب يُحتجز الرصيد دون حجز العمولة. **التشخيص**: create الطلب يخزن `amount=net`؛ الاحتجاز (wd_hold) كان يخصم `net`؛ الاعتماد (المسار غير المحتجز) كان يخصم `amount` (net)؛ فالعمولة (fee=gross−net) بقيت برصيد المستخدم في كل المسارات. كانت كامنة لأن `withdraw_fee_percent` الافتراضي 0 (net=gross). آثار مرافقة: بطاقة التأكيد كانت تعرض خصماً بالإجمالي لا يطابق الفعلي، وKPI كان يحسب fee إيراداً لم يُحصَّل.
**الإصلاح**: (1) الاحتجاز يخصم `wdc_gross` + معاملة wd_hold بالإجمالي؛ (2) الرفض يسترد الإجمالي (gross من النوتة مع fallback للـamount للطلبات القديمة)؛ (3) الاعتماد غير المحتجز يخصم gross؛ (4) أوامر الدفع (payouts) والمحرك الآلي تبقى بالصافي — المستفيد يستلم صافيه دائماً؛ (5) بطاقة التأكيد صارت صحيحة تلقائياً (تعرض الإجمالي الذي سيُخصم فعلاً). **الفحص الجديد t13**: احتجز 100@5% ⇒ رصيد 0 + دفع 95 (العمولة 5 حُصِّلت)؛ رفض محتجز 50 ⇒ استرداد 50؛ اعتماد غير محتجز 20/19 ⇒ رصيد −20 + دفع 19. ملاحظة تطوير: خطأ سيناريو بالفحص نفسه (احتجاز من صفر أعطى سالباً) ظهر ثم صُحح — الكود كان سليماً. المجموعة 308 فحصاً. AUTO-WD يبقى متوقفاً بقرار المالك.

## 44) ترقيع [PMLINK — 5.18.17] زر مال اللاعب المنهار + نافذة حارس المحفظة الزمنية
**مصدر البنود**: قائمة مراجعة خارجية عرضها المالك (5 بنود) — فُحصت ضد الكود قبل التنفيذ: 3 حقيقية نُفذت، بند «ترتيب الاحتجاز مع الاعتماد التلقائي» لا أساس له في كودنا (لا اعتماداً تلقائياً للسحوبات)، وبند ROUND في SQL أُجل بقرار المالك.
**الإصلاحات**: (1) `_panel_money_start` كانت تستدعي `PanelMoneyFSM.username.set()` بنمط aiogram v2 — خاصية غير موجودة في v3 (مؤكد تجريبياً) فكان أول ضغطة إيداع/سحب لاعب ينهار بAttributeError؛ صارت `state.set_state(...)` مثل باقي الكود. (2) `cb_yes` كان يبني callback باسم اللاعب الخام — تجاوز 64 بايت يرفض تلجرام الزر، وأي «:» بالاسم يكسر التفسير؛ صار معقّماً من «:» ومقصوصاً إلى 12 حرفاً ضمن الحد. (3) `_wgt_expected_balance`: الشحنات مشروطة بـ`processed_at > last_ts` بينما مدفوعات شام تُخصم بلا شرط زمني — ازدواج خصم لما قبل الطابع وإنذارات فجوة كاذبة؛ أُضيف `AND processed_at > ?`.
**لماذا لم تلتقطها الفحوص سابقاً**: مسار مال اللاعب يتطلب لوحة مربوطة (غير مربوطة عمداً بالبيئة) فكان عيباً كامناً — الفحص الجديد t14 يغطيه ساكنةً وحياً (نافذة معزولة: قديمة قبل الطابع تُهمل، جديدة بعده تُخصم). المجموعة 309 فحصاً.

